---
phase: 03-action-engine
plan: 09
type: execute
wave: 2
depends_on: [07]
files_modified:
  - src/features/actions/actions/create-actions.ts
  - src/lib/action-worker/expiry.ts
autonomous: true
gap_closure: true
requirements:
  - ACTN-04
  - ACTN-10

must_haves:
  truths:
    - "New DM actions created via createActionsFromSignal have expires_at set to 12 hours from creation"
    - "The expiry cron's threshold is 12 hours (matches the create-time value)"
    - "create-time expiry and cron threshold are documented as a single shared constant OR both use the literal 12 hours consistently"
  artifacts:
    - path: "src/features/actions/actions/create-actions.ts"
      provides: "DM action creation with 12h expiry"
      contains: "12 * 60 * 60 * 1000"
    - path: "src/lib/action-worker/expiry.ts"
      provides: "Cron expiry logic at 12h threshold"
      contains: "12 * 60 * 60 * 1000"
  key_links:
    - from: "create-actions.ts expires_at"
      to: "expiry.ts threshold"
      via: "same 12h constant"
      pattern: "12\\s*\\*\\s*60\\s*\\*\\s*60\\s*\\*\\s*1000"

---

<objective>
Close UAT Gap 3 (MAJOR): DM expiry in create-actions.ts is hardcoded to 4h but phase spec (03-05 summary + UAT) requires 12h. Additionally, expiry.ts currently uses 4h — verify and align to 12h so the cron threshold matches creation expiry.

Purpose: Users check their queue roughly once per day; 4h is too aggressive and causes drafts to disappear before they can be reviewed. 12h matches CONTEXT.md locked decision ("12-hour expiry: cards silently disappear from queue when expired").

Output: Both create-actions.ts and expiry.ts use 12h threshold, consistent with phase spec.
</objective>

<execution_context>
@C:/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-action-engine/03-CONTEXT.md
@.planning/phases/03-action-engine/03-UAT.md
@.planning/phases/03-action-engine/03-05-SUMMARY.md
@src/features/actions/actions/create-actions.ts
@src/lib/action-worker/expiry.ts

<interfaces>
<!-- Current state of both files: -->
<!-- create-actions.ts line 95-96: -->
<!--   const expiresAt = new Date(Date.now() + 4 * 60 * 60 * 1000).toISOString() -->
<!-- expiry.ts line 13-15: -->
<!--   const fourHoursAgo = new Date(Date.now() - 4 * 60 * 60 * 1000).toISOString() -->
<!-- Target: both use 12 * 60 * 60 * 1000 (12 hours in milliseconds). -->
</interfaces>

<rules>
- CONTEXT.md locked decision: "12-hour expiry: cards silently disappear from queue when expired". This is the source of truth.
- Do NOT refactor into a shared constant file unless trivial — keep the change as minimal as possible.
- Do NOT touch cron schedule in vercel.json (cron runs hourly — 12h threshold means cards live 12-13h in practice, still well under stale).
</rules>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change DM expiry to 12h in create-actions.ts</name>
  <files>src/features/actions/actions/create-actions.ts</files>
  <read_first>
    - src/features/actions/actions/create-actions.ts (full file — 117 lines)
    - .planning/phases/03-action-engine/03-CONTEXT.md (confirm "12-hour expiry" locked decision)
  </read_first>
  <action>
In `src/features/actions/actions/create-actions.ts`, change line 95-97 from:
```typescript
const expiresAt = new Date(
  Date.now() + 4 * 60 * 60 * 1000,
).toISOString()
```

to:
```typescript
// 12h expiry per phase CONTEXT decision (ACTN-10 aligned with queue-check cadence)
const expiresAt = new Date(
  Date.now() + 12 * 60 * 60 * 1000,
).toISOString()
```

Keep the surrounding code unchanged. Do not touch other parts of this file.
  </action>
  <verify>
    <automated>
grep -n "12 \* 60 \* 60 \* 1000" src/features/actions/actions/create-actions.ts
# Expect 1 match on or near the expiresAt assignment.
grep -n "4 \* 60 \* 60 \* 1000" src/features/actions/actions/create-actions.ts
# Expect 0 matches (no stale 4h literal remaining).
pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
- `grep -c "12 \\* 60 \\* 60 \\* 1000" src/features/actions/actions/create-actions.ts` returns exactly 1.
- `grep -c "4 \\* 60 \\* 60 \\* 1000" src/features/actions/actions/create-actions.ts` returns 0.
- File still exports `createActionsFromSignal` (unchanged signature).
- `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>New DM drafts get 12h expiry at creation.</done>
</task>

<task type="auto">
  <name>Task 2: Align expiry cron threshold to 12h in expiry.ts</name>
  <files>src/lib/action-worker/expiry.ts</files>
  <read_first>
    - src/lib/action-worker/expiry.ts (full file — 55 lines)
    - src/features/actions/actions/create-actions.ts (verify Task 1 applied first)
  </read_first>
  <action>
In `src/lib/action-worker/expiry.ts`:

1. Update the header comment (lines 1-5) to reflect 12h:
```typescript
/**
 * Expiry logic for stale pending_approval actions.
 *
 * Actions older than 12 hours that are still pending_approval
 * are marked as expired, and their prospects are reset to detected.
 */
```

2. Change lines 13-15 from:
```typescript
const fourHoursAgo = new Date(
  Date.now() - 4 * 60 * 60 * 1000,
).toISOString()
```

to:
```typescript
const twelveHoursAgo = new Date(
  Date.now() - 12 * 60 * 60 * 1000,
).toISOString()
```

3. Rename the variable reference on line 22 from `fourHoursAgo` to `twelveHoursAgo`:
```typescript
.lt("created_at", twelveHoursAgo)
```

Do not change function signatures, return shape, or surrounding error handling.
  </action>
  <verify>
    <automated>
grep -n "12 \* 60 \* 60 \* 1000" src/lib/action-worker/expiry.ts
# Expect 1 match.
grep -n "4 \* 60 \* 60 \* 1000" src/lib/action-worker/expiry.ts
# Expect 0 matches.
grep -n "fourHoursAgo" src/lib/action-worker/expiry.ts
# Expect 0 matches.
grep -n "twelveHoursAgo" src/lib/action-worker/expiry.ts
# Expect 2 matches: the const + the .lt(...) usage.
pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
- `grep -c "12 \\* 60 \\* 60 \\* 1000" src/lib/action-worker/expiry.ts` returns 1.
- `grep -c "4 \\* 60 \\* 60 \\* 1000" src/lib/action-worker/expiry.ts` returns 0.
- `grep -c "fourHoursAgo" src/lib/action-worker/expiry.ts` returns 0.
- `grep -c "twelveHoursAgo" src/lib/action-worker/expiry.ts` returns 2.
- `pnpm typecheck` exits 0.
- File's exported `expireStaleActions` signature unchanged.
  </acceptance_criteria>
  <done>Cron expiry threshold matches creation-time expiry (both 12h).</done>
</task>

</tasks>

<verification>
Optional runtime smoke: call createActionsFromSignal with a test signal, read back the inserted actions row, confirm expires_at - created_at ≈ 12h. (Not required — grep + typecheck is sufficient for this deterministic constant change.)
</verification>

<success_criteria>
- create-actions.ts uses 12h expiry.
- expiry.ts uses 12h threshold.
- No stale 4h literals remain in either file.
- Typecheck passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-action-engine/03-09-SUMMARY.md` noting the two-line constant change and why (CONTEXT.md locked decision: 12-hour expiry).
</output>
