---
phase: 05-billing-onboarding-growth
plan: 07
type: execute
wave: 3
depends_on: ["05-04", "05-06"]
files_modified:
  - src/app/api/og/results-card/route.tsx
  - src/features/growth/components/results-card.tsx
  - src/features/growth/components/share-buttons.tsx
  - src/app/api/cron/digest/route.ts
  - src/app/(app)/page.tsx
autonomous: true
requirements: [GROW-04, GROW-05, GROW-06]

must_haves:
  truths:
    - "System generates 1200x630 PNG results card with weekly stats"
    - "User can download results card image and share to X/LinkedIn"
    - "Daily email digest sends at 8:00 user timezone with signal count, top signal, pending DMs"
    - "Results card and digest show real user data"
  artifacts:
    - path: "src/app/api/og/results-card/route.tsx"
      provides: "OG image generation for results card"
      contains: "ImageResponse"
    - path: "src/features/growth/components/results-card.tsx"
      provides: "Results card preview with share buttons"
    - path: "src/app/api/cron/digest/route.ts"
      provides: "Daily digest email cron"
      contains: "CRON_SECRET"
  key_links:
    - from: "src/app/api/og/results-card/route.tsx"
      to: "next/og"
      via: "ImageResponse for PNG generation"
      pattern: "ImageResponse"
    - from: "src/app/api/cron/digest/route.ts"
      to: "resend"
      via: "Resend SDK for email sending"
      pattern: "resend.emails.send"
---

<objective>
Build the weekly results card image generator with sharing, and the daily email digest cron with signal/DM stats.

Purpose: Results card sharing turns users into distribution channels; daily digest drives re-engagement and approval velocity.
Output: Results card PNG generator, share buttons, daily digest cron.
</objective>

<execution_context>
@C:/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-billing-onboarding-growth/05-CONTEXT.md
@.planning/phases/05-billing-onboarding-growth/05-UI-SPEC.md
@.planning/phases/05-billing-onboarding-growth/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Results card OG image generation and sharing UI</name>
  <files>
    src/app/api/og/results-card/route.tsx
    src/features/growth/components/results-card.tsx
    src/features/growth/components/share-buttons.tsx
    src/app/(app)/page.tsx
  </files>
  <read_first>
    - src/app/(app)/page.tsx
    - .planning/phases/05-billing-onboarding-growth/05-UI-SPEC.md
    - .planning/phases/05-billing-onboarding-growth/05-RESEARCH.md
  </read_first>
  <action>
1. Create `src/app/api/og/results-card/route.tsx`:
- Import { ImageResponse } from "next/og"
- GET handler, reads stats from searchParams:
  - scanned (posts scanned this week)
  - signals (intent signals detected)
  - dms (DMs sent)
  - replies (replies received)
  - replyRate (reply percentage)
  - conversions (conversions count)
- Return new ImageResponse with 1200x630 dimensions
- Card design using ONLY flexbox (no CSS grid -- Satori limitation):
  - Dark background: linear-gradient from stone-900 (#1c1917) to stone-800 (#292524)
  - "repco weekly" title in white, 36px, top-left
  - 6 stat boxes in 2 rows of 3 (flexbox wrap):
    - Each box: stat value (48px, white, bold) + label (16px, stone-400)
    - Labels: "Posts scanned", "Signals", "DMs sent", "Replies", "Reply rate", "Conversions"
  - repco.ai watermark bottom-right, stone-500, 14px
  - Indigo accent line (4px height) at top
- Set headers: Content-Type image/png, Cache-Control public max-age=3600

2. Create `src/features/growth/components/share-buttons.tsx` ("use client"):
- Props: imageUrl (string), stats (object with scanned, signals, dms, replies, replyRate, conversions)
- Three buttons:
  - "Download image": fetch imageUrl, create blob URL, trigger download with filename "repco-weekly-{date}.png"
  - "Share to X": window.open with pre-filled tweet text:
    `This week repco found {signals} people looking for my product and sent {dms} DMs with a {replyRate}% reply rate`
    Include image URL in tweet
  - "Share to LinkedIn": window.open with LinkedIn share URL:
    `https://www.linkedin.com/sharing/share-offsite/?url={encodedUrl}`
    Pre-fill text: "This week my AI sales rep found {signals} potential customers and achieved a {replyRate}% reply rate. Fully automated."
- Buttons styled with secondary variant, icons: Download, X logo (or generic Share icon), LinkedIn logo

3. Create `src/features/growth/components/results-card.tsx` ("use client"):
- Props: stats object
- Renders a preview of the results card (HTML version matching the OG image design)
- Card with dark background, same 2x3 stat layout as OG image
- Below card: ShareButtons component
- "repco weekly" title at top

4. Update `src/app/(app)/page.tsx`:
- Add ResultsCard to dashboard
- Query weekly stats: count signals detected in last 7 days, count actions completed (DMs) in last 7 days, count replies in last 7 days, calculate reply rate
- Build imageUrl: `/api/og/results-card?scanned=X&signals=X&dms=X&replies=X&replyRate=X&conversions=X`
- Render ResultsCard component in dashboard grid (after credit card and prospect stats)
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/og/results-card/route.tsx imports ImageResponse from "next/og"
    - src/app/api/og/results-card/route.tsx returns ImageResponse with width: 1200, height: 630
    - src/app/api/og/results-card/route.tsx uses only flexbox (display: "flex"), no "grid" or "position: absolute"
    - src/features/growth/components/share-buttons.tsx has "Download image", "Share to X", "Share to LinkedIn" buttons
    - src/features/growth/components/share-buttons.tsx opens window for X sharing with pre-filled tweet text
    - src/features/growth/components/results-card.tsx renders stat preview with "repco weekly" title
    - src/app/(app)/page.tsx renders ResultsCard with weekly stats query
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Results card OG image endpoint (1200x630 PNG), HTML preview component, share buttons (download, X, LinkedIn) with pre-filled posts, dashboard integration</done>
</task>

<task type="auto">
  <name>Task 2: Daily email digest cron</name>
  <files>
    src/app/api/cron/digest/route.ts
  </files>
  <read_first>
    - src/app/api/cron/zombie-recovery/route.ts
    - src/app/api/cron/credit-burn/route.ts
    - src/lib/logger.ts
    - supabase/migrations/00002_initial_schema.sql
  </read_first>
  <action>
Create `src/app/api/cron/digest/route.ts`:
- GET handler following cron pattern: CRON_SECRET bearer auth, correlationId, structured logging
- Service role Supabase client
- Query all users where subscription_active = true OR trial_ends_at > now()
- For each user (check user timezone if available, default UTC; only send if current hour in user timezone is 8):
  1. Query intent_signals count from last 24h for user
  2. Query top signal (highest intent_strength) from last 24h
  3. Query count of actions with status = 'pending_approval' for user
  4. Query user's product_profiles.name for product reference
  5. Build email content:
     - Subject: "{signalCount} people looking for {productName} yesterday"
     - Body (plain HTML for now, can use React Email if resend is available):
       - Greeting with product name
       - "{signalCount} intent signals detected in the last 24 hours"
       - If top signal: "Top signal: [post excerpt truncated to 100 chars] (intent: {strength}/10)"
       - If pending DMs: "{pendingCount} DMs waiting for your approval"
       - CTA: "View dashboard" linking to NEXT_PUBLIC_SITE_URL
  6. Send via Resend (if available) or log email content to job_logs (fallback if resend not configured)
     - Use RESEND_API_KEY env var, skip sending if not configured
     - From: "repco <noreply@repco.ai>" (or configured domain)
     - To: user.email
- Log per-user send status to job_logs with metadata
- await logger.flush()
- Return { sent: N, skipped: N, failed: N }
- Add to vercel.json cron: schedule "0 * * * *" (every hour, timezone check happens in code)
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/cron/digest/route.ts has GET handler with Bearer token check against CRON_SECRET
    - Contains query for intent_signals count from last 24h
    - Contains query for pending_approval actions count
    - Contains timezone check (current hour === 8 in user timezone)
    - Contains email subject template "{signalCount} people looking for {productName} yesterday"
    - Contains Resend send call or fallback logging
    - Contains logger.flush() before return
    - Returns JSON with sent, skipped, failed counts
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Daily email digest cron sends at 8:00 user timezone with signal count, top signal details, and pending DM count</done>
</task>

</tasks>

<verification>
- pnpm typecheck passes
- /api/og/results-card?scanned=100&signals=10&dms=5&replies=2&replyRate=40&conversions=1 returns PNG
- Dashboard shows results card with share buttons
- /api/cron/digest endpoint returns valid JSON
</verification>

<success_criteria>
- Results card generates 1200x630 PNG with 6 stat boxes on dark background
- Share to X opens pre-filled tweet with stats
- Share to LinkedIn opens pre-filled post with stats
- Download button triggers PNG file download
- Daily digest cron sends to active users at 8:00 their timezone
- Digest email includes signal count, top signal, pending DMs
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-onboarding-growth/05-07-SUMMARY.md`
</output>
