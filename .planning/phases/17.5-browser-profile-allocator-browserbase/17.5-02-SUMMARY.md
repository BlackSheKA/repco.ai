---
phase: 17.5
plan: 02
subsystem: browser-profile-allocator
tags: [browserbase, allocator, account-actions, ui, iframe]
requires: [17.5-01]
provides: [browserbase-client, allocator-v2, connect-flow-iframe]
affects: [worker.ts, cron/check-replies, cron/linkedin-prescreen, executor-tests]
tech-stack:
  added: ["@browserbasehq/sdk@^2"]
  patterns: ["lazy-singleton-client", "404-swallow", "iframe-embed", "refcount-cleanup"]
key-files:
  created:
    - src/lib/browserbase/client.ts
    - src/lib/browserbase/__tests__/client.test.ts
  modified:
    - .env.example
    - src/features/browser-profiles/lib/allocator.ts
    - src/features/browser-profiles/lib/get-browser-profile.ts
    - src/features/accounts/actions/account-actions.ts
    - src/features/accounts/components/connection-flow.tsx
    - src/features/accounts/components/account-list.tsx
    - src/features/accounts/lib/types.ts
    - src/lib/action-worker/worker.ts
    - src/app/api/cron/check-replies/route.ts
    - src/app/api/cron/linkedin-prescreen/route.ts
    - src/app/api/cron/check-replies/__tests__/route.test.ts
    - src/lib/action-worker/__tests__/worker-quarantine.test.ts
    - src/lib/action-worker/__tests__/worker-linkedin-followup.test.ts
decisions:
  - "Browserbase REST via @browserbasehq/sdk default export; lazy module-level singleton"
  - "BrowserProfile.gologin_* kept as deprecated optional fields until plan 17.5-03 refits worker/cron consumers"
  - "Allocator preserves D-02 reuse query verbatim and D-10 best-effort rollback (deleteContext + browser_profiles delete)"
  - "stopAccountBrowser is a no-op — Browserbase sessions auto-release on timeout, persistent context auto-saves cookies"
metrics:
  duration: ~25 min
  completed: 2026-04-27
---

# Phase 17.5 Plan 02: Client + Allocator + ConnectFlow Summary

End-to-end Browserbase wire-up at the allocator + UI layer. New REST client, allocator rewritten preserving D-02/D-09/D-10, server actions emit D-11 copy and never leak debuggerFullscreenUrl, ConnectionFlow embeds Browserbase live view in a sandboxed iframe per UI-SPEC.

## Tasks Completed

| Task | Commit | Files |
|------|--------|-------|
| 1 — Browserbase client + env-guard tests | `76f9a65` | client.ts, client.test.ts, .env.example |
| 2 — Allocator + get-browser-profile rewrite | `b872e0d` | allocator.ts, get-browser-profile.ts, types.ts |
| 3 — Refit account-actions (4 server actions + types) | `b7023a1` | account-actions.ts |
| 4 — ConnectionFlow iframe-embed UI | `9bf9777` | connection-flow.tsx + transitional fixes |

(SDK install was committed earlier as `bef279f` outside this plan run.)

## Verification

- `pnpm typecheck` — exit 0 (project-wide)
- `pnpm test src/lib/browserbase/__tests__/client.test.ts` — 2/2 passed
- `pnpm lint src/features/accounts/components/connection-flow.tsx` — clean
- `grep "gologin"` in scope files — only doc-comments (predecessor reference)
- `grep "console."` in src/lib/browserbase/client.ts — 1 match (the "no console.log" rule comment); zero runtime calls
- `grep "debuggerFullscreenUrl"` paired with `console.|logger.|toast(` — 0 hits

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] account-list.tsx referenced result.profileId**
- **Found during:** Task 3 typecheck
- **Issue:** ConnectAccount return rename (profileId → contextId) broke direct caller in account-list.tsx (out-of-scope but a single-file blocker for plan-level typecheck)
- **Fix:** Renamed both occurrences to `result.contextId`
- **Files modified:** src/features/accounts/components/account-list.tsx
- **Commit:** `9bf9777`

**2. [Rule 3 — Blocking] BrowserProfile type cascade across plan-03 territory**
- **Found during:** Task 4 typecheck
- **Issue:** Renaming `BrowserProfile.gologin_profile_id` to `browserbase_context_id` propagated typecheck failures into worker.ts, 2 cron routes, and 3 executor test fixtures — files explicitly owned by plan 17.5-03
- **Fix:** Transitional shim — kept `gologin_profile_id` and `gologin_proxy_id` on BrowserProfile as `@deprecated optional`, added `browserbase_context_id: string` (required), added 1-line `?? ""` fallbacks at the 3 GoLogin caller sites with comments referencing plan 17.5-03 for full rewrite, added `browserbase_context_id` literal to 3 mock-fixture builders
- **Rationale:** Without these tactical edits, plan 02's typecheck criterion is unsatisfiable until plan 03 ships. The shims are 1 line each, fully reverted by plan 03's worker.ts refit.
- **Files modified:** types.ts, worker.ts, check-replies/route.ts, linkedin-prescreen/route.ts, 3 test fixtures
- **Commit:** `9bf9777`

**3. [Rule 3 — Blocking] connectAccount discriminated union too tight**
- **Found during:** Task 3 typecheck
- **Issue:** Initial `Promise<{success:true,...} | {success?:false,error}>` shape forced consumers to narrow before reading fields; account-list.tsx pre-existing pattern reads `result.error` and `result.accountId` without narrowing
- **Fix:** Loosened to a single object type with all fields optional (matches surrounding codebase convention for server actions)
- **Files modified:** src/features/accounts/actions/account-actions.ts
- **Commit:** `b7023a1`

**4. [Rule 3 — Lint] direct setState in useEffect**
- **Found during:** Task 4 lint
- **Issue:** ESLint rule `react-hooks/set-state-in-effect` flagged the reset-state calls before the async IIFE inside the start effect
- **Fix:** Moved reset state into the explicit `retry()` callback (which calls them and bumps a nonce). The effect body is now only the async IIFE.
- **Files modified:** src/features/accounts/components/connection-flow.tsx
- **Commit:** `9bf9777`

## Threat Model Coverage

| Threat | Mitigation Verified |
|--------|---------------------|
| T-17.5-01 (BROWSERBASE_API_KEY disclosure) | Server-only env var, no NEXT_PUBLIC_ prefix, lazy singleton, errors throw plain "BROWSERBASE_API_KEY not set" without echoing the value |
| T-17.5-03 (debuggerFullscreenUrl exfiltration) | Generated server-side, never logged in client.ts/account-actions.ts/connection-flow.tsx, error paths render D-11 copy not the URL, session timeout 1800s bounds exposure |
| T-17-02-01 (handle injection) | Zod validation at server-action boundary preserved (no schema change) |
| T-17-02-02 (cross-user reuse) | D-02 reuse query is user_id-scoped; RLS enforces row-level access |

## Self-Check: PASSED

- `src/lib/browserbase/client.ts` — FOUND
- `src/lib/browserbase/__tests__/client.test.ts` — FOUND
- Commit `76f9a65` — FOUND
- Commit `b872e0d` — FOUND
- Commit `b7023a1` — FOUND
- Commit `9bf9777` — FOUND

## Hand-off to Plan 17.5-03

Plan 03 rewrites worker.ts and the 2 cron routes to use Browserbase + Stagehand. When plan 03 lands, it MUST also:
1. Tighten `BrowserProfile` — drop `gologin_profile_id` and `gologin_proxy_id` deprecated fields
2. Remove the 3 transitional `?? ""` fallbacks (they will become dead code once `connectToProfile` is replaced)
3. Update the 3 test fixtures — drop the deprecated fields, keep `browserbase_context_id`
4. Delete `src/lib/gologin/client.ts`
