---
phase: 17.5
plan: 04
type: execute
wave: 4
depends_on: ["17.5-03"]
files_modified:
  - .env.example
  - vercel.json
  - .planning/phases/17-residential-proxy-gologin-profile-allocator/*-SUMMARY.md
  - screenshots/uat-17.5-*.png (CREATED)
  - .planning/phases/17.5-browser-profile-allocator-browserbase/17.5-04-UAT-RUNBOOK.md (CREATED)
autonomous: false
requirements: [BPRX-03, BPRX-05, BPRX-06]
requirements_addressed: [BPRX-03, BPRX-05, BPRX-06]
threat_model:
  - T-17.5-01
  - T-17.5-02
  - T-17.5-03
must_haves:
  truths:
    - "All 6 UAT scenarios from VALIDATION.md Manual-Only Verifications pass with screenshot evidence"
    - "GOLOGIN_API_TOKEN removed from .env.example and vercel.json"
    - "BROWSERBASE_API_KEY and BROWSERBASE_PROJECT_ID added to Vercel production env (manual step documented)"
    - "Phase 17 SUMMARY files marked superseded_by: 17.5"
    - "Stagehand action success rate ≥95% (≥4/5 prospects per executor) on UAT smoke run"
  artifacts:
    - path: "screenshots/uat-17.5-iframe-renders.png"
      provides: "UAT scenario 1 evidence"
    - path: "screenshots/uat-17.5-cookies-persist.png"
      provides: "UAT scenario 2 evidence"
    - path: ".planning/phases/17.5-browser-profile-allocator-browserbase/17.5-04-UAT-RUNBOOK.md"
      provides: "Step-by-step UAT runbook with results"
  key_links:
    - from: "Phase 17.5 final state"
      to: "Production env (Vercel)"
      via: "manual env var update + main branch merge"
      pattern: "vercel env add BROWSERBASE_API_KEY production"
---

<objective>
Run 6 manual UAT scenarios against the dev branch (Browserbase live), capture screenshots, finalize env cleanup, and mark Phase 17 superseded.

Purpose: Empirical proof that vendor swap satisfies BPRX-03/05/06 + Stagehand reliability target. Final hand-off to production deploy gate.
Output: UAT runbook with PASS/FAIL annotations, screenshots in `screenshots/`, clean env files, Phase 17 SUMMARYs annotated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.5-browser-profile-allocator-browserbase/17.5-CONTEXT.md
@.planning/phases/17.5-browser-profile-allocator-browserbase/17.5-RESEARCH.md
@.planning/phases/17.5-browser-profile-allocator-browserbase/17.5-VALIDATION.md
@.planning/phases/17.5-browser-profile-allocator-browserbase/17.5-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [HUMAN] Execute 6 UAT scenarios + Stagehand smoke run; capture screenshots</name>
  <read_first>
    - .planning/phases/17.5-browser-profile-allocator-browserbase/17.5-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/17.5-browser-profile-allocator-browserbase/17.5-CONTEXT.md (D17.5-05 #9 — 6 scenarios; D17.5-06 — Stagehand reliability target)
    - CLAUDE.md (port 3001 + dev branch effppfiphrykllkpkdbv + screenshots/ directory rule)
    - C:\Users\kamil\.claude\projects\c--Users-kamil-Code-repco-ai\memory\feedback_self_test_uat.md (run autonomously; pause only on true blockers)
  </read_first>
  <what-built>
    Phase 17.5 vendor swap: Browserbase contexts replace GoLogin profiles; Stagehand absorbs LinkedIn DOM volatility; ConnectionFlow uses iframe embed. Plans 01-03 are complete and committed.
  </what-built>
  <how-to-verify>
    Pre-flight:
    1. Confirm `.env.local` has `BROWSERBASE_API_KEY` (live `bb_live_...`) and `BROWSERBASE_PROJECT_ID=8220f64d-131b-47bd-8202-d0eb2fb45003`.
    2. Confirm `.env.local` Supabase points to dev branch `effppfiphrykllkpkdbv` (per CLAUDE.md "local must point at dev").
    3. Start: `pnpm dev --port 3001`.
    4. Sign in to repco at `http://localhost:3001`.
    5. Create file `.planning/phases/17.5-browser-profile-allocator-browserbase/17.5-04-UAT-RUNBOOK.md` with one section per scenario; record PASS/FAIL + screenshot path.

    **Scenario 1: Iframe renders + accepts clicks** (D17.5-05 #7, T-17.5-03 spot-check)
    - /accounts → "Add Reddit account"
    - Confirm iframe loads with Browserbase debugger inside; spinner overlay disappears on iframe `onLoad`.
    - Click inside the iframe → keystrokes register → form input accepts text.
    - Screenshot: `screenshots/uat-17.5-iframe-renders.png`.
    - **Verify**: open browser DevTools → Network tab; confirm `debuggerFullscreenUrl` is NOT logged in console + NOT shown in any toast text. PASS only if URL is invisible to user-facing UI strings.

    **Scenario 2: Cookies persist across sessions** (D17.5-09)
    - Complete LinkedIn login inside iframe (real account).
    - Click "I've logged in" → step 2 verifying → step 3 verified.
    - Reload `/accounts`; click "Open browser" again on same account.
    - Confirm Reddit/LinkedIn lands logged-in WITHOUT re-entering credentials.
    - Screenshot: `screenshots/uat-17.5-cookies-persist.png`.

    **Scenario 3: Reddit network-security check passes** (spike finding)
    - Connect Reddit account fresh.
    - Inside iframe, navigate to reddit.com → confirm NO hard CAPTCHA wall (soft CAPTCHA acceptable).
    - Screenshot: `screenshots/uat-17.5-reddit-no-hardblock.png`.

    **Scenario 4: 7-country geolocation** (BPRX-05)
    - D-01 hardcodes `country = "US"` everywhere in app code; DO NOT edit `connectAccount` call sites or any production code path to pass a different country (risks accidental commit + violates D-01).
    - For each ISO-2 in [US, GB, DE, PL, FR, CA, AU], seed a `browser_profiles` row directly on the dev Supabase branch via Supabase Management API, then call `startAccountBrowser` against that profile id.
    - Exact INSERT SQL (run via curl `--ssl-no-revoke` against dev branch `effppfiphrykllkpkdbv` per CLAUDE.md):
      ```sql
      INSERT INTO browser_profiles (id, user_id, browserbase_context_id, country_code, timezone, locale, display_name)
      VALUES (gen_random_uuid(), '{test_user_uuid}', 'ctx_uat_{COUNTRY}_' || extract(epoch from now())::text, '{COUNTRY}', '{tz_from_country_map}', '{locale_from_country_map}', 'UAT geo {COUNTRY}')
      RETURNING id;
      ```
      For the `browserbase_context_id` value: first call `createContext()` via a Node REPL using `@browserbasehq/sdk` to get a real context id (the placeholder above is illustrative — Browserbase rejects fake context ids on session create).
    - Look up the inserted row id, then in the app temporarily start a session against that browser_profile id (e.g. via a Node script that imports `createSession` directly, OR via a one-off route handler scoped to UAT — DO NOT modify connectAccount or its call sites).
    - In iframe navigate to `https://api.ipify.org` to read IP.
    - Cross-check geolocation at `https://ipinfo.io/{ip}/json`; record country.
    - PASS only if geolocated country matches expected.
    - After all 7 countries verified: `DELETE FROM browser_profiles WHERE display_name LIKE 'UAT geo %'` and call `deleteContext` on each created Browserbase context to avoid orphan rows.
    - Screenshot: `screenshots/uat-17.5-geo-{COUNTRY}.png` per country.


    **Scenario 5: Reuse rule across platforms** (BPRX-06 / D-02)
    - Create test user.
    - Connect Reddit account (US) → connect LinkedIn account (US, same user).
    - SQL on dev branch via Supabase Management API:
      ```sql
      SELECT count(*) FROM browser_profiles WHERE user_id = '{uid}' AND country_code = 'US';
      ```
    - Expected: 1 row.
    - SQL: `SELECT count(*) FROM social_accounts WHERE user_id = '{uid}'` → expected: 2.
    - Connect SECOND LinkedIn account (US) → expected browser_profiles count = 2 (new context for same-platform second account per D-02).
    - Screenshot: `screenshots/uat-17.5-reuse-rule.png` (DB query result).

    **Scenario 6: Rollback on INSERT failure** (D-10)
    - Use a transaction-rollback approach so RLS state is NEVER persisted on the dev branch:
      ```sql
      BEGIN;
      -- Capture baseline policy names for verification (expected 3 from 00003_rls_policies.sql)
      SELECT policyname FROM pg_policies WHERE tablename='social_accounts' ORDER BY policyname;
      -- Expected output (exactly these 3):
      --   Users can create own social accounts
      --   Users can read own social accounts
      --   Users can update own social accounts
      -- Inject synthetic failure: drop the INSERT policy inside the transaction
      DROP POLICY "Users can create own social accounts" ON social_accounts;
      ```
    - In a separate browser session (still inside the same DB transaction is not possible from the app — instead: use a session-scoped pgbouncer pin OR run the connectAccount attempt against a dev branch where the DROP has been committed, then ROLLBACK after). Simplest path: run the DROP, attempt connectAccount via app (expect failure surfacing D-11 copy), then run `ROLLBACK;` from the same psql/Mgmt-API session to restore policies.
    - Verify NO orphan context remains: Node REPL `await bb.contexts.list({ projectId: process.env.BROWSERBASE_PROJECT_ID })` — confirm no context created in the last 60s for the failed attempt.
    - Post-rollback verification (MANDATORY before exiting scenario):
      ```sql
      SELECT policyname FROM pg_policies WHERE tablename='social_accounts' ORDER BY policyname;
      ```
      Expected output identical to baseline above (3 policies, names match exactly). PASS the scenario only if this matches.
    - Screenshot: `screenshots/uat-17.5-rollback.png` (capture both pg_policies queries side-by-side + connectAccount error toast).


    **Scenario 7: Stagehand action success rate per LinkedIn executor** (D17.5-06)
    - Run 5 known-good prospects per executor (DM, Connect, Follow, Like, Comment).
    - Record pass/fail per attempt in runbook.
    - Threshold: ≥4/5 PASS per executor → acceptable.
    - <4/5 → flag for Sonnet 4.6 fallback (CONTEXT.md D17.5-06 contingency).
    - Screenshot of action-worker logs: `screenshots/uat-17.5-stagehand-success-rate.png`.

    All screenshots MUST go in `screenshots/` (CLAUDE.md rule). Filename pattern: `screenshots/uat-17.5-{scenario}.png`.
  </how-to-verify>
  <resume-signal>Type "approved" with runbook path, OR describe failures with screenshot evidence. If Stagehand <4/5 on any executor, document and decide: (a) bump that executor to Sonnet 4.6 in a follow-up, (b) accept and ship.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Clean .env.example + vercel.json (remove GOLOGIN_API_TOKEN)</name>
  <read_first>
    - .env.example (current — has GOLOGIN_API_TOKEN block)
    - vercel.json (current — verify if GOLOGIN_API_TOKEN referenced)
  </read_first>
  <files>.env.example, vercel.json</files>
  <action>
    1. Remove all `GOLOGIN_*` lines from `.env.example`. Confirm `BROWSERBASE_API_KEY` and `BROWSERBASE_PROJECT_ID` lines (added in plan 02 task 1) remain.

    2. Remove any `GOLOGIN_API_TOKEN` reference from `vercel.json` (env array, build env, etc.). If `vercel.json` had no env block, no change needed — verify via grep.

    3. Final sweep: `grep -rin "GOLOGIN\|gologin" .env.example vercel.json` returns 0 hits.
  </action>
  <verify>
    <automated>! grep -in "gologin" .env.example vercel.json && grep -c "BROWSERBASE_API_KEY" .env.example && grep -c "BROWSERBASE_PROJECT_ID" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` has 0 `gologin` references
    - `vercel.json` has 0 `gologin` references
    - `.env.example` contains `BROWSERBASE_API_KEY` and `BROWSERBASE_PROJECT_ID` lines
  </acceptance_criteria>
  <done>Env files cleaned; only Browserbase env vars remain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [HUMAN] Update Vercel production env vars (cannot be automated from a plan)</name>
  <what>
    Add `BROWSERBASE_API_KEY` and `BROWSERBASE_PROJECT_ID` to Vercel production scope. Remove `GOLOGIN_API_TOKEN` from production scope.

    These are real secrets; per CLAUDE.md they live ONLY in Vercel (never on disk as `.env.production.local`). The user must execute these commands locally because they touch production secrets.

    Commands (run from project root):
    ```bash
    # Add Browserbase secrets (production scope)
    vercel env add BROWSERBASE_API_KEY production
    # paste the live key when prompted: bb_live_...

    vercel env add BROWSERBASE_PROJECT_ID production
    # paste: 8220f64d-131b-47bd-8202-d0eb2fb45003

    # Remove GoLogin secret from production scope
    vercel env rm GOLOGIN_API_TOKEN production
    # confirm with `y`
    ```

    Verify with `vercel env ls production` — expected: BROWSERBASE_API_KEY + BROWSERBASE_PROJECT_ID present, GOLOGIN_API_TOKEN absent.

    DO NOT push to main yet — that's a separate gate (`/deploy-to-production`). This task only updates the env scope so the next prod deploy has correct secrets.
  </what>
  <resume-signal>Type "envs-updated" with `vercel env ls production` output (redact actual key values). If any vercel env command fails (auth, scope, etc.), describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Mark Phase 17 SUMMARYs as superseded_by: 17.5</name>
  <read_first>
    - .planning/phases/17-residential-proxy-gologin-profile-allocator/ (list all *-SUMMARY.md files)
    - .planning/phases/17.5-browser-profile-allocator-browserbase/17.5-CONTEXT.md (D17.5-08 — keep history; mark superseded)
  </read_first>
  <files>.planning/phases/17-residential-proxy-gologin-profile-allocator/*-SUMMARY.md</files>
  <action>
    For each `*-SUMMARY.md` file in `.planning/phases/17-residential-proxy-gologin-profile-allocator/`:
    - Add `superseded_by: 17.5` to the YAML frontmatter (between existing fields).
    - Add a short top-of-body note: `> **Superseded by Phase 17.5** (Browserbase pivot). See `.planning/phases/17.5-browser-profile-allocator-browserbase/` for the working implementation. This summary preserved as historical evidence of the GoLogin pivot per D17.5-08.`
    - Do NOT delete content — D17.5-08 explicitly preserves git history and the Phase 17 narrative.

    The Phase 17 commits on `development` branch (3e8aefc, 27ff9ea, 45e7262, 56afb8f, 1586057) stay untouched per D17.5-08.
  </action>
  <verify>
    <automated>for f in .planning/phases/17-residential-proxy-gologin-profile-allocator/*-SUMMARY.md; do grep -q "superseded_by: 17.5" "$f" || echo "MISSING in $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - Every Phase 17 SUMMARY file has `superseded_by: 17.5` in frontmatter
    - Every Phase 17 SUMMARY file has the superseded note at top of body
    - No Phase 17 file content deleted (git diff shows only additions)
  </acceptance_criteria>
  <done>Phase 17 history preserved with cross-reference to 17.5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production Vercel scope ↔ developer machine | Secrets enter via `vercel env add`; never written to disk |
| UAT screenshots → repository | Screenshots may capture session debugger URLs visually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.5-01 | I (Information Disclosure) | BROWSERBASE_API_KEY in Vercel production | mitigate | Added via `vercel env add`, never written to local files; CLAUDE.md prohibits `.env.production.local`; redact in `vercel env ls` output when documenting |
| T-17.5-03 | I (Information Disclosure) | UAT screenshots may show debuggerFullscreenUrl in browser address bar | mitigate | Iframe is embedded in repco app — debug URL is NOT in the parent page address bar; screenshots focus on iframe content and DB queries; reviewer should crop/blur any incidental debug-URL leaks before commit |
| T-17.5-02 | T (Prompt Injection) | Stagehand action success rate UAT | mitigate | Scenario 7 explicitly measures this; <4/5 escalates to Sonnet 4.6 fallback; user-supplied text confirmed not in page.act args (verified in plan 03) |
| T-17.5-UAT-01 | T (Tampering) | Synthetic RLS failure injection (Scenario 6) | mitigate | RLS revert is part of the runbook; verify post-test via `pg_policies` query that 4 RLS policies are present and identical to baseline |
</threat_model>

<verification>
- All 6 UAT scenarios documented in `17.5-04-UAT-RUNBOOK.md` with PASS markers + screenshot paths
- Stagehand smoke run ≥4/5 per executor (or fallback decision documented)
- `grep -rin "gologin" .env.example vercel.json` returns 0 hits
- Vercel production env updated (manual confirmation captured)
- All Phase 17 SUMMARY files annotated with `superseded_by: 17.5`
</verification>

<success_criteria>
- BPRX-03 proven: residential proxy attached, IP geolocates per country
- BPRX-05 proven: 7-country whitelist works end-to-end
- BPRX-06 proven: D-02 reuse rule observed in DB state
- D-10 rollback proven via synthetic failure
- Iframe interaction works for real users
- Cookies persist across sessions (D17.5-09)
- Phase 17 history preserved + cross-referenced
- Production env ready for next `/deploy-to-production` gate
</success_criteria>

<output>
After completion, create `.planning/phases/17.5-browser-profile-allocator-browserbase/17.5-04-SUMMARY.md` summarizing UAT results, env state, and any Stagehand fallback decisions.
</output>
