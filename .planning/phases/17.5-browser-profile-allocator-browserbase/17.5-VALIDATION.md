---
phase: 17.5
slug: browser-profile-allocator-browserbase
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 17.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Per CONTEXT.md D-16, mocked allocator unit tests are deferred — UAT covers correctness. Pure functions (`country-map.ts`) and lightweight code-level invariants get automated checks.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.x |
| **Config file** | `vitest.config.ts` (existing) |
| **Quick run command** | `pnpm test src/lib/browserbase src/features/browser-profiles` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~30 seconds (full), ~3 seconds (quick) |
| **UAT runner** | manual via `pnpm dev --port 3001` (per CLAUDE.md, dev branch Supabase) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test` scoped to changed file's nearest `__tests__` (or `pnpm typecheck` for files without tests).
- **After every plan wave:** Run `pnpm test` (full vitest suite) + `pnpm typecheck` + `pnpm lint`.
- **Before `/gsd-verify-work`:** Full suite green AND 6 manual UAT scenarios executed (see Manual-Only Verifications).
- **Max feedback latency:** 30 seconds for full suite.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 17.5-01-01 | 01 | 1 | BPRX-03,05,06 | — | TRUNCATE only on dev; columns swap atomically | manual | apply 00025 to dev branch via supabase mgmt API; `\d browser_profiles` shows `browserbase_context_id text UNIQUE NOT NULL` and no `gologin_*` cols | ✅ existing | ⬜ pending |
| 17.5-02-01 | 02 | 2 | BPRX-03,05 | T-17.5-01 | API key never echoed; server-only | unit | `pnpm test src/lib/browserbase/__tests__/client.test.ts` (env-var guard test) | ❌ W0 | ⬜ pending |
| 17.5-02-02 | 02 | 2 | BPRX-06 | — | reuse rule preserved; rollback intact | code-grep | `grep -in "occupied\|order.*created_at.*ASC" src/features/browser-profiles/lib/allocator.ts` returns hits | ✅ existing | ⬜ pending |
| 17.5-02-03 | 02 | 2 | BPRX-05 | — | country-map unchanged | unit | `pnpm test src/features/browser-profiles/lib/__tests__/country-map.test.ts` | ✅ existing | ⬜ pending |
| 17.5-02-04 | 02 | 2 | — | T-17.5-03 | debugger URL never logged | code-grep | `grep -rn "debuggerFullscreenUrl\|debugger_url" src/ \| grep -i "console\.\|logger\." returns 0 lines` | ✅ existing | ⬜ pending |
| 17.5-03-01 | 03 | 3 | — | T-17.5-02 | Stagehand only sees scoped element text | code-review | `grep -rn "page\.act\|page\.extract" src/lib/action-worker/actions/` does NOT include user-supplied prospect message text in `act` arg | ✅ existing | ⬜ pending |
| 17.5-03-02 | 03 | 3 | — | — | gologin/ deleted | shell | `test ! -d src/lib/gologin && grep -rL gologin src/ \| wc -l` matches expected (only doc + .planning/) | ✅ existing | ⬜ pending |
| 17.5-03-03 | 03 | 3 | — | — | typecheck passes after gologin removal | typecheck | `pnpm typecheck` exits 0 | ✅ existing | ⬜ pending |
| 17.5-04-01 | 04 | 4 | BPRX-03,05,06 | T-17.5-01,02,03 | UAT scenarios documented + screenshots committed | manual | UAT runbook execution; screenshots in `screenshots/uat-17.5-*.png` per CLAUDE.md | ⚠️ runbook | ⬜ pending |
| 17.5-04-02 | 04 | 4 | — | T-17.5-01 | env vars cleaned | shell | `grep -n "GOLOGIN\|gologin" .env.example vercel.json` returns 0 hits; `grep BROWSERBASE .env.example` returns expected 2 entries | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/browserbase/__tests__/client.test.ts` — env-var guard tests (BROWSERBASE_API_KEY required, BROWSERBASE_PROJECT_ID required). SDK NOT mocked (env-var guard fires before SDK constructor — mocking would bypass guard); verifies error messages thrown directly from client lazy-init helpers.
- [ ] `vitest.config.ts` — already exists, no changes.
- [ ] No new framework install needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Iframe live view renders + accepts clicks/typing | D17.5-05 #7 | Real browser interaction; can't be automated cheaply | `pnpm dev --port 3001` → /accounts → "Add Reddit account" → confirm iframe loads → click inside → type in login form → submit |
| Cookies persist across sessions | D17.5-09 | Multi-session real-world flow | Complete LinkedIn login in iframe → click "Verify session" → reload page → click "Open browser" again → should see logged-in state without re-entering credentials |
| Reddit network-security check passes | spike finding | Cannot mock the actual Reddit anti-bot signal | Connect Reddit account → land on Reddit homepage; verify NO hard-block CAPTCHA; soft CAPTCHA may appear (acceptable per spike) |
| Geolocation: 7 country codes work | BPRX-05 | Real proxy IP must geolocate per country | For each of US, GB, DE, PL, FR, CA, AU: connect a fresh test account → in iframe navigate to https://api.ipify.org → cross-check IP at https://ipinfo.io/{ip}/json → country matches |
| Reuse rule across platforms | BPRX-06 (D-02) | Real DB state | Connect Reddit account (US) → connect LinkedIn account (US) → SQL: `SELECT count(*) FROM browser_profiles WHERE user_id = '{uid}' AND country_code = 'US'` returns 1 |
| Rollback on INSERT failure | D-10 | Synthetic injection of failure path | Temporarily break RLS for social_accounts insert → attempt connectAccount → after error, query Browserbase contexts list via SDK → no orphan with this user's handle visible |
| Stagehand action success rate per executor | D17.5-06 | Real LinkedIn DOM | Run UAT runbook with 5 known-good profiles per executor (DM, connect, follow, like, comment); record pass count; ≥4/5 acceptable, <4/5 escalate to Sonnet 4.6 |

---

## Validation Sign-Off

- [ ] All tasks have automated verify OR documented manual UAT step
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers env-var guard test for new client
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter after planner expands per-task tests

**Approval:** pending
