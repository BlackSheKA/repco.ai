---
phase: 17.5
plan: "04"
type: uat-runbook
status: in-progress
author: claude+kamil
date_started: 2026-04-28
---

# Phase 17.5 Plan 04 — UAT Runbook

UAT verifies BPRX-03/05/06 + Stagehand reliability for the GoLogin → Browserbase pivot. 7 scenarios + Stagehand smoke run.

**Environment**:
- Dev branch Supabase: `effppfiphrykllkpkdbv`
- App: `pnpm dev --port 3001` (already running)
- Browserbase project: `8220f64d-131b-47bd-8202-d0eb2fb45003`
- Test user: `claude-test-1777152298@repco.test` (id `2909f58b-077d-4980-bb8e-a10d283e797c`)

## Status Summary

| # | Scenario | Owner | Status | Evidence |
|---|---|---|---|---|
| 1 | Iframe renders + accepts clicks | kamil | TODO | `screenshots/uat-17.5-iframe-renders.png` |
| 2 | Cookies persist across sessions | kamil | TODO | `screenshots/uat-17.5-cookies-persist.png` |
| 3 | Reddit network-security check passes | kamil | TODO | `screenshots/uat-17.5-reddit-no-hardblock.png` |
| 4 | 7-country geolocation (BPRX-05) | claude | **PASS 7/7** | `scripts/uat-17.5-04-geolocation.mjs` JSON output below |
| 5 | Reuse rule across platforms (BPRX-06 / D-02) | claude | **PASS 3/3** | `scripts/uat-17.5-04-allocator.mjs` JSON output below |
| 6 | Rollback on INSERT failure (D-10) | claude | **PASS** (with finding ⚠) | `scripts/uat-17.5-04-allocator.mjs` JSON output below |
| 7 | Stagehand action success rate | kamil | TODO | `screenshots/uat-17.5-stagehand-success-rate.png` |

---

## Scenario 4 — 7-country geolocation (BPRX-05) — PASS

**Method**: Created BB context per country with `proxies: [{ type: "browserbase", geolocation: { country } }]`, ran CDP `Runtime.evaluate` to fetch `api.ipify.org` then `ipinfo.io` from inside the proxied browser, compared observed country to expected.

**Run**: `node --env-file=.env.local scripts/uat-17.5-04-geolocation.mjs` (50 seconds, 7/7 PASS)

| Country | IP | Observed country | Region/City | Pass |
|---|---|---|---|---|
| US | 50.25.51.21 | US | West Virginia / Kermit | ✅ |
| GB | 51.7.108.82 | GB | England / London | ✅ |
| DE | 93.221.164.139 | DE | Hesse / Kassel | ✅ |
| PL | 79.191.184.252 | PL | Mazovia / Warsaw | ✅ |
| FR | 37.67.104.20 | FR | Île-de-France / Conflans-Sainte-Honorine | ✅ |
| CA | 64.227.210.49 | CA | Alberta / Spirit River | ✅ |
| AU | 175.32.131.25 | AU | New South Wales / Sydney | ✅ |

**No screenshots captured** (`screenshots/uat-17.5-geo-{COUNTRY}.png` deferred — automated proof via API is stronger evidence than browser screenshots, and the script self-cleans contexts). PASS criterion: observed country matches expected for all 7. Met.

**Cleanup**: All 7 contexts deleted (raw `DELETE /v1/contexts/{id}` — see Scenario 6 finding).

---

## Scenario 5 — Reuse rule across platforms (BPRX-06 / D-02) — PASS

**Method**: Replicated `allocateBrowserProfile` logic via service-role client + real Browserbase contexts, exercising the reuse + new-context branches. Cleaned up test user state before and after.

**Run**: `node --env-file=.env.local scripts/uat-17.5-04-allocator.mjs`

| Step | Action | Reused? | bp_count | sa_count | Expected | Pass |
|------|--------|---------|----------|----------|----------|------|
| 1 | `allocate(reddit, US)` | false | 1 | 1 | bp=1, sa=1, NEW | ✅ |
| 2 | `allocate(linkedin, US)` (same user) | true | 1 | 2 | bp=1, sa=2, REUSED | ✅ |
| 3 | `allocate(linkedin, US)` #2 | false | 2 | 3 | bp=2, sa=3, NEW (D-02 platform conflict) | ✅ |

D-02 chokepoint: `social_accounts.browser_profile_id` is unique per `(profile, platform)` via the reuse-lookup query that excludes already-occupied profiles. Step 2 reuses bp from step 1 (no LinkedIn yet). Step 3 cannot reuse (LinkedIn already on bp 1) → allocates a new context.

---

## Scenario 6 — Rollback on INSERT failure (D-10) — PASS with finding ⚠

**Method**: Plan called for an RLS DROP inside a transaction, but the Supabase Management API runs each statement in a fresh connection — `SET LOCAL ROLE` + `DROP POLICY` + `INSERT` + `ROLLBACK` cannot share one transaction. Substituted an equivalent failure mode: passed an invalid enum value (`'invalid_platform_for_d10_test'`) for `social_accounts.platform` to trigger `22P02` (`invalid_input_value`). This drives the same rollback branch in `allocator.ts:181-192` (the `saErr` arm).

**Run**: same script as Scenario 5 — final stage.

| Check | Expected | Observed | Pass |
|------|----------|----------|------|
| Postgres error code | `22P02` | `22P02` | ✅ |
| `browser_profiles` row removed | bp_count = 0 | 0 | ✅ |
| `social_accounts` row absent | sa_count = 0 | 0 | ✅ |
| Browserbase context deleted | `GET /v1/contexts/{id}` → 404 | 404 | ✅ |

### ⚠ Production finding — Browserbase SDK `contexts.delete` is broken

While building the orphan-check probe, discovered that `@browserbasehq/sdk@2.10.0`'s `bb.contexts.delete(id)` always returns:
```
400 BadRequestError: Body cannot be empty when content-type is set to 'application/json'
```
The SDK injects `Content-Type: application/json` on a body-less DELETE; the BB API server rejects it. Verified with two independent runs (a fresh single-context create+delete probe, and the geolocation script's cleanup loop).

A raw `fetch("https://api.browserbase.com/v1/contexts/{id}", { method: "DELETE", headers: { "X-BB-API-Key": ... } })` returns 204 cleanly.

**Production impact**: `src/lib/browserbase/client.ts:56-63` (`deleteContext`) wraps the SDK call. Its callers in `src/features/browser-profiles/lib/allocator.ts:125,189` use `.catch(() => {})` — so the 400 is silently swallowed, the function returns "ok", but the BB context is **never actually deleted**. Every D-10 rollback in production leaks one Browserbase context. Same applies to any user-initiated delete/cleanup path that funnels through `deleteContext`.

The UAT here passes because the runner uses a raw-fetch `rawDeleteContext()` workaround, NOT the production code path. Surfaced as a follow-up — see `## Follow-ups` below.

---

## Scenario 1 — Iframe renders + accepts clicks (kamil)

**Pre-flight**: `pnpm dev --port 3001` is running (verified).

**Steps for kamil**:
1. Open `http://localhost:3001/accounts`.
2. Click "Add Reddit account" (or "Add LinkedIn account").
3. Fill handle, click submit → ConnectionFlow modal opens with iframe.
4. Confirm:
   - Spinner overlay disappears on iframe `onLoad`.
   - Click inside iframe registers; type some text into the in-iframe form.
5. Open DevTools → Console + Network tabs. Confirm `debuggerFullscreenUrl` is NOT printed in any console message and NOT visible in any toast text.
6. Screenshot the modal: `screenshots/uat-17.5-iframe-renders.png`.

**PASS criteria**: iframe interactive + URL not surfaced anywhere user-facing.

---

## Scenario 2 — Cookies persist across sessions (kamil)

**Steps**:
1. Inside the iframe from Scenario 1, complete a real LinkedIn login.
2. Click "I've logged in" → step 2 verifying → step 3 verified.
3. Reload `/accounts`. Click "Open browser" again on the same account.
4. Confirm the iframe lands on linkedin.com **already logged in** — no credential prompt.
5. Screenshot: `screenshots/uat-17.5-cookies-persist.png`.

**PASS criteria**: persistent context preserves cookies across BB sessions.

---

## Scenario 3 — Reddit network-security check passes (kamil)

**Steps**:
1. Connect a fresh Reddit account.
2. Inside the iframe, navigate to `https://reddit.com`.
3. Confirm: no hard CAPTCHA wall (soft CAPTCHA is acceptable).
4. Screenshot: `screenshots/uat-17.5-reddit-no-hardblock.png`.

**PASS criteria**: Reddit loads without blocking the BB IP/fingerprint combination.

---

## Scenario 7 — Stagehand action success rate per LinkedIn executor (kamil)

5 known-good prospects per executor (DM, Connect, Follow, Like, Comment). PASS threshold: ≥4/5 per executor.

| Executor | Prospects 1–5 | Pass count | Threshold | Action |
|---|---|---|---|---|
| linkedin-dm | _ / _ / _ / _ / _ | _/5 | ≥4/5 | TODO |
| linkedin-connect | _ / _ / _ / _ / _ | _/5 | ≥4/5 | TODO |
| linkedin-follow | _ / _ / _ / _ / _ | _/5 | ≥4/5 | TODO |
| linkedin-like | _ / _ / _ / _ / _ | _/5 | ≥4/5 | TODO |
| linkedin-comment | _ / _ / _ / _ / _ | _/5 | ≥4/5 | TODO |

If any executor < 4/5 → bump to Sonnet 4.6 (CONTEXT.md D17.5-06 contingency) or accept and ship.

Screenshot of action-worker logs: `screenshots/uat-17.5-stagehand-success-rate.png`.

---

## Task 3 — Vercel production env (kamil)

```bash
vercel env add BROWSERBASE_API_KEY production         # paste bb_live_...
vercel env add BROWSERBASE_PROJECT_ID production      # paste 8220f64d-131b-47bd-8202-d0eb2fb45003
vercel env rm GOLOGIN_API_TOKEN production            # confirm y if present
vercel env ls production                              # capture redacted output
```

---

## Follow-ups

1. ~~**BB SDK `contexts.delete` 400 bug**~~ — **FIXED in this plan**.
   - `src/lib/browserbase/client.ts:deleteContext` now uses raw `fetch` DELETE with `X-BB-API-Key` header only (no Content-Type). Returns 204 cleanly. Verified E2E: create → delete → probe returns 404.
   - Test added: `deleteContext throws when BROWSERBASE_API_KEY missing`.

2. UAT scenarios 4/5/6 evidence is JSON, not screenshots — strictly the plan asked for `.png` files. Stronger evidence (programmatic with full input/output captured) is intentional, but if the original screenshot artefacts are required for compliance, run the scripts again with `--evidence-dir screenshots/` and capture `console.log` to `screenshots/uat-17.5-{scenario}.log`. The current artefacts are inline in this runbook.
