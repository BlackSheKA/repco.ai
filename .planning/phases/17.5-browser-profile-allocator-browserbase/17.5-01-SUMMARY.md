---
phase: 17.5
plan: 01
subsystem: database / browser-profiles
tags: [migration, browserbase, schema, gologin-removal]
requires: []
provides:
  - "browser_profiles.browserbase_context_id column (text UNIQUE NOT NULL)"
  - "drop of browser_profiles.gologin_profile_id and gologin_proxy_id"
  - "tabula rasa truncate of browser_profiles + cascading social_accounts"
affects:
  - "supabase/migrations/00025_browserbase_columns.sql"
  - "Supabase dev branch effppfiphrykllkpkdbv (live schema)"
tech-stack:
  added: []
  patterns:
    - "TRUNCATE CASCADE for tabula rasa pre-launch wipes (project_users_are_test_data)"
key-files:
  created:
    - supabase/migrations/00025_browserbase_columns.sql
  modified: []
decisions:
  - "Migration filename 00025 (not 00024 as CONTEXT.md said) — 00024_mechanism_costs.sql already exists"
  - "TRUNCATE CASCADE preferred over DELETE — atomic, faster, explicit on dev"
  - "RLS policies untouched — column drop/add does not affect user_id-keyed policies"
metrics:
  duration: ~3 min
  completed: 2026-04-27
requirements_addressed: [BPRX-03, BPRX-05, BPRX-06]
---

# Phase 17.5 Plan 01: Schema Migration Summary

Phase 17.5 GoLogin → Browserbase column swap migration committed and applied to dev branch.

## What was built

- New migration file `supabase/migrations/00025_browserbase_columns.sql` containing the three-step sequence:
  1. `TRUNCATE browser_profiles CASCADE` (clears test rows + dependent `social_accounts` via FK CASCADE).
  2. `ALTER TABLE browser_profiles DROP COLUMN gologin_profile_id, DROP COLUMN gologin_proxy_id`.
  3. `ALTER TABLE browser_profiles ADD COLUMN browserbase_context_id text UNIQUE NOT NULL`.
- Migration applied to dev branch `effppfiphrykllkpkdbv` via Supabase Management API (HTTP 201).

## Verification results (live schema on dev branch)

Column projection:
```
id (uuid, NO), user_id (uuid, NO), country_code (text, NO),
timezone (text, NO), locale (text, NO), display_name (text, YES),
created_at (timestamptz, YES), browserbase_context_id (text, NO)
```
- `browserbase_context_id` present with `data_type=text`, `is_nullable=NO`.
- No `gologin_profile_id` / `gologin_proxy_id` columns remain.

Constraints:
- `browser_profiles_browserbase_context_id_key` (UNIQUE) — confirms the new UNIQUE constraint.
- `browser_profiles_pkey` (PRIMARY) and `browser_profiles_user_id_fkey` (FK) preserved.

RLS policies (all 4 carried over from 00023):
- "Users can read own browser profiles"
- "Users can create own browser profiles"
- "Users can update own browser profiles"
- "Users can delete own browser profiles"

`pnpm typecheck` clean.

## Deviations from Plan

None. Plan executed exactly as written. CONTEXT.md said 00024 but the plan itself already corrected to 00025 — no in-flight deviation.

## Commits

- `a144540` — feat(17.5): add 00025 browserbase_columns migration

## Self-Check: PASSED

- `supabase/migrations/00025_browserbase_columns.sql` — FOUND
- Commit `a144540` — FOUND in `git log`
- Live schema on `effppfiphrykllkpkdbv` confirms column swap, UNIQUE constraint, and 4 RLS policies preserved
