---
phase: 03-action-engine
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/computer-use/executor.ts
  - src/lib/computer-use/screenshot.ts
  - src/lib/computer-use/actions/reddit-dm.ts
  - src/lib/computer-use/actions/reddit-engage.ts
  - src/lib/action-worker/worker.ts
  - src/lib/action-worker/claim.ts
  - src/lib/action-worker/limits.ts
  - src/app/api/webhooks/actions/route.ts
  - src/app/api/cron/expire-actions/route.ts
  - src/lib/computer-use/__tests__/stuck-detection.test.ts
  - src/lib/action-worker/__tests__/claim.test.ts
  - src/lib/action-worker/__tests__/limits.test.ts
  - src/lib/action-worker/__tests__/expiry.test.ts
autonomous: true
requirements: [ACTN-01, ACTN-04, ACTN-05, ACTN-06, ACTN-07, ACTN-08, ACTN-09, ACTN-10]

must_haves:
  truths:
    - "Approved actions execute via DB Webhook -> Vercel Function -> GoLogin -> Playwright CDP -> Haiku CU"
    - "Haiku CU is capped at 15 steps with stuck detection (3 identical screenshots = abort)"
    - "Screenshots are uploaded to Supabase Storage after action execution"
    - "FOR UPDATE SKIP LOCKED prevents duplicate action execution"
    - "Daily action limits are enforced atomically before execution"
    - "Expired actions (12h) are cleaned up by cron"
    - "Engage actions (like, follow) auto-execute without approval"
    - "Anti-ban modules (delays, noise, target isolation, warmup gate, active hours) are called in the worker pipeline"
  artifacts:
    - path: "src/lib/computer-use/executor.ts"
      provides: "Haiku CU agent loop with step cap and stuck detection"
      exports: ["executeCUAction"]
    - path: "src/lib/computer-use/screenshot.ts"
      provides: "Screenshot capture, comparison, and Supabase Storage upload"
      exports: ["captureScreenshot", "isStuck", "uploadScreenshot"]
    - path: "src/lib/action-worker/worker.ts"
      provides: "Main action execution pipeline orchestrator with anti-ban wiring"
      exports: ["executeAction"]
    - path: "src/lib/action-worker/claim.ts"
      provides: "Atomic action claiming via Supabase RPC"
      exports: ["claimAction"]
    - path: "src/app/api/webhooks/actions/route.ts"
      provides: "DB Webhook handler for action execution"
      exports: ["POST"]
    - path: "src/app/api/cron/expire-actions/route.ts"
      provides: "Cron endpoint to expire stale actions"
      exports: ["GET"]
    - path: "src/lib/computer-use/__tests__/stuck-detection.test.ts"
      provides: "Tests for isStuck screenshot comparison"
    - path: "src/lib/action-worker/__tests__/claim.test.ts"
      provides: "Tests for claimAction RPC wrapper"
    - path: "src/lib/action-worker/__tests__/limits.test.ts"
      provides: "Tests for checkAndIncrementLimit"
    - path: "src/lib/action-worker/__tests__/expiry.test.ts"
      provides: "Tests for expiry cron logic"
  key_links:
    - from: "src/app/api/webhooks/actions/route.ts"
      to: "src/lib/action-worker/worker.ts"
      via: "import executeAction"
      pattern: "executeAction"
    - from: "src/lib/action-worker/worker.ts"
      to: "src/lib/computer-use/executor.ts"
      via: "import executeCUAction"
      pattern: "executeCUAction"
    - from: "src/lib/action-worker/worker.ts"
      to: "src/lib/gologin/adapter.ts"
      via: "import connectToProfile"
      pattern: "connectToProfile"
    - from: "src/lib/action-worker/claim.ts"
      to: "supabase RPC"
      via: "supabase.rpc('claim_action')"
      pattern: "claim_action"
    - from: "src/lib/action-worker/worker.ts"
      to: "src/lib/action-worker/delays.ts"
      via: "import randomDelay, sleep, isWithinActiveHours"
      pattern: "randomDelay|isWithinActiveHours"
    - from: "src/lib/action-worker/worker.ts"
      to: "src/lib/action-worker/noise.ts"
      via: "import shouldInjectNoise, generateNoiseActions"
      pattern: "shouldInjectNoise|generateNoiseActions"
    - from: "src/lib/action-worker/worker.ts"
      to: "src/lib/action-worker/target-isolation.ts"
      via: "import checkAndAssignTarget"
      pattern: "checkAndAssignTarget"
    - from: "src/lib/action-worker/worker.ts"
      to: "src/features/accounts/lib/types.ts"
      via: "import getWarmupState"
      pattern: "getWarmupState"
---

<objective>
Build the complete action execution pipeline: Haiku CU executor, action worker, webhook handler, daily limits, and expiry cron. Wire anti-ban modules (from 03-04) into the worker pipeline. Create unit tests for claim, limits, stuck detection, and expiry.

Purpose: This is the core execution engine -- the pipeline that turns an approved DM into an actual sent message via GoLogin + Playwright + Haiku Computer Use. It also handles engage actions (like/follow) that auto-execute without approval. Anti-ban modules are called at each step to prevent detection.

Output: CU executor, screenshot management, action worker pipeline with anti-ban wiring, webhook handler, expiry cron, 4 test files.
</objective>

<execution_context>
@C:/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-action-engine/03-RESEARCH.md
@.planning/phases/03-action-engine/03-01-SUMMARY.md

<interfaces>
From src/features/actions/lib/types.ts (created in 03-01):
```typescript
export interface Action { id, user_id, prospect_id, account_id, action_type, status, drafted_content, final_content, ... }
export interface CUResult { success: boolean; steps: number; screenshots: string[]; error?: string }
```

From src/lib/gologin/adapter.ts (created in 03-01):
```typescript
export async function connectToProfile(profileId: string): Promise<GoLoginConnection>
export async function disconnectProfile(browser: Browser): Promise<void>
```

From src/features/accounts/lib/types.ts (created in 03-01):
```typescript
export type HealthStatus = "warmup" | "healthy" | "warning" | "cooldown" | "banned"
export interface SocialAccount { ..., cooldown_until: string | null, ... }
export function getWarmupState(warmupDay: number, completedAt: string | null): WarmupState
```

From src/lib/action-worker/delays.ts (created in 03-04, Wave 2 parallel):
```typescript
export function randomDelay(mean?: number, std?: number, min?: number): number
export function sleep(seconds: number): Promise<void>
export function isWithinActiveHours(timezone: string, activeStart: number, activeEnd: number): boolean
```

From src/lib/action-worker/noise.ts (created in 03-04, Wave 2 parallel):
```typescript
export function shouldInjectNoise(): boolean
export function generateNoiseActions(): string[]
```

From src/lib/action-worker/target-isolation.ts (created in 03-04, Wave 2 parallel):
```typescript
export async function checkAndAssignTarget(supabase: SupabaseClient, prospectId: string, accountId: string): Promise<{ allowed: boolean; error?: string }>
```

From supabase/migrations/00006_phase3_action_engine.sql (created in 03-01):
```sql
-- claim_action(p_action_id uuid) RETURNS SETOF actions
-- check_and_increment_limit(p_account_id uuid, p_action_type text) RETURNS boolean
```

From src/lib/logger.ts (Phase 1):
```typescript
export const logger: { info, warn, error, flush }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Haiku CU executor + screenshot management + stuck detection tests</name>
  <files>
    src/lib/computer-use/executor.ts
    src/lib/computer-use/screenshot.ts
    src/lib/computer-use/actions/reddit-dm.ts
    src/lib/computer-use/actions/reddit-engage.ts
    src/lib/computer-use/__tests__/stuck-detection.test.ts
  </files>
  <read_first>
    - .planning/phases/03-action-engine/03-RESEARCH.md
    - src/features/actions/lib/types.ts
    - src/lib/gologin/adapter.ts
    - vitest.config.ts
  </read_first>
  <action>
**1. Create `src/lib/computer-use/__tests__/stuck-detection.test.ts`:**

Write tests for the `isStuck` function BEFORE implementation:
- Test: fewer than 3 screenshots returns false
- Test: 3 identical base64 strings returns true
- Test: 3 different base64 strings returns false
- Test: last 3 identical but earlier ones different returns true
- Test: 2 identical + 1 different returns false

Run tests -- all must fail (RED).

**2. Create `src/lib/computer-use/screenshot.ts`:**

```typescript
import type { Page } from "playwright-core"
import { createClient } from "@supabase/supabase-js"

export async function captureScreenshot(page: Page): Promise<string> {
  // page.screenshot({ type: "png" }) -> base64 string
  // Resize to 1024x768 if larger (Anthropic CU constraint)
}

export function isStuck(screenshots: string[]): boolean {
  // If fewer than 3 screenshots, return false
  // Compare last 3 screenshots by Buffer equality (base64 string comparison)
  // Return true if all 3 are identical
}

export async function uploadScreenshot(
  actionId: string,
  screenshotBase64: string,
  step: number,
): Promise<string | null> {
  // Create service role Supabase client inline:
  // createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!)
  // Upload to bucket "screenshots" at path `actions/${actionId}/step-${step}.png`
  // Buffer.from(screenshotBase64, "base64")
  // contentType: "image/png", upsert: true
  // Return signed URL (7 day expiry) using supabase.storage.from("screenshots").createSignedUrl(path, 86400 * 7)
  // Return null on error
  // NOTE: Use createSignedUrl (NOT getPublicUrl) -- screenshots bucket is private
}
```

Run stuck-detection tests -- all must pass (GREEN).

**3. Create `src/lib/computer-use/executor.ts`:**

```typescript
import Anthropic from "@anthropic-ai/sdk"
import type { Page } from "playwright-core"
import { captureScreenshot, isStuck } from "./screenshot"
import type { CUResult } from "@/features/actions/lib/types"

const MAX_STEPS = 15
const BETA_HEADER = "computer-use-2025-01-24"

export async function executeCUAction(
  page: Page,
  prompt: string,
): Promise<CUResult> {
  const client = new Anthropic()
  const screenshots: string[] = []
  let steps = 0

  const initialScreenshot = await captureScreenshot(page)
  screenshots.push(initialScreenshot)

  // Build initial messages array with image + text
  // Agent loop: while (steps < MAX_STEPS)
  //   Call client.beta.messages.create with:
  //     model: "claude-haiku-4-5-20251001"
  //     max_tokens: 1024
  //     tools: [{ type: "computer_20250124", name: "computer", display_width_px: 1024, display_height_px: 768 }]
  //     betas: [BETA_HEADER]
  //   Process tool_use blocks -> executeComputerAction(page, toolUse.input)
  //   Take screenshot after each action
  //   Check isStuck(screenshots) -> abort if true
  //   If no tool_use blocks -> task complete, break
  // Return { success: steps < MAX_STEPS && !stuck, steps, screenshots, error? }
}

async function executeComputerAction(page: Page, input: Record<string, unknown>): Promise<void> {
  // Map CU actions to Playwright:
  // action "click" -> page.mouse.click(input.x, input.y)
  // action "type" -> page.keyboard.type(input.text)
  // action "key" -> page.keyboard.press(input.key)
  // action "scroll" -> page.mouse.wheel(0, input.delta_y ?? 300)
  // action "screenshot" -> no-op (handled by loop)
  // action "move" -> page.mouse.move(input.x, input.y)
  // action "wait" -> new Promise(resolve => setTimeout(resolve, (input.duration ?? 1) * 1000))
  // Add small random delay (100-500ms) after each action for natural behavior
}
```

**4. Create `src/lib/computer-use/actions/reddit-dm.ts`:**

```typescript
export function getRedditDMPrompt(recipientHandle: string, messageContent: string): string {
  return `You are on Reddit. Send a direct message to user "${recipientHandle}".

Steps:
1. Click the chat/message icon in the top navigation bar
2. Click "New Message" or the compose button
3. In the "To" field, type "${recipientHandle}"
4. Wait for the username suggestion to appear, then click on it
5. In the message body field, type the following message exactly:

${messageContent}

6. Click the "Send" button
7. Verify the message was sent (confirmation appears)

Do not add any extra text. Send exactly the message above.`
}
```

**5. Create `src/lib/computer-use/actions/reddit-engage.ts`:**

```typescript
export function getRedditLikePrompt(postUrl: string): string {
  return `You are on Reddit viewing: ${postUrl}
Click the upvote button on this post. The upvote arrow should turn orange/red after clicking.`
}

export function getRedditFollowPrompt(userHandle: string): string {
  return `You are on Reddit. Follow user "${userHandle}".
1. Navigate to https://www.reddit.com/user/${userHandle}/
2. Click the "Follow" button on their profile
3. Verify the button changes to "Following"`
}
```
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/computer-use/__tests__/stuck-detection.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/computer-use/executor.ts exports `executeCUAction`
    - executor.ts contains `MAX_STEPS = 15`
    - executor.ts contains `claude-haiku-4-5-20251001`
    - executor.ts contains `computer-use-2025-01-24`
    - executor.ts contains `computer_20250124`
    - executor.ts contains `display_width_px: 1024`
    - src/lib/computer-use/screenshot.ts exports `captureScreenshot`, `isStuck`, `uploadScreenshot`
    - screenshot.ts contains `isStuck` function comparing last 3 screenshots
    - screenshot.ts uses `createSignedUrl` (NOT `getPublicUrl`) for private bucket
    - src/lib/computer-use/actions/reddit-dm.ts exports `getRedditDMPrompt`
    - src/lib/computer-use/actions/reddit-engage.ts exports `getRedditLikePrompt`, `getRedditFollowPrompt`
    - src/lib/computer-use/__tests__/stuck-detection.test.ts has 5+ test cases
    - All stuck-detection tests pass
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>CU executor with 15-step cap + stuck detection, screenshot capture/compare/upload with signed URLs, Reddit action prompts, stuck detection tests all passing</done>
</task>

<task type="auto">
  <name>Task 2: Action worker pipeline with anti-ban wiring + webhook handler + limits + expiry cron + tests</name>
  <files>
    src/lib/action-worker/worker.ts
    src/lib/action-worker/claim.ts
    src/lib/action-worker/limits.ts
    src/app/api/webhooks/actions/route.ts
    src/app/api/cron/expire-actions/route.ts
    src/lib/action-worker/__tests__/claim.test.ts
    src/lib/action-worker/__tests__/limits.test.ts
    src/lib/action-worker/__tests__/expiry.test.ts
  </files>
  <read_first>
    - src/lib/computer-use/executor.ts
    - src/lib/computer-use/screenshot.ts
    - src/lib/gologin/adapter.ts
    - src/lib/gologin/client.ts
    - src/features/actions/lib/types.ts
    - src/features/accounts/lib/types.ts
    - src/lib/action-worker/delays.ts
    - src/lib/action-worker/noise.ts
    - src/lib/action-worker/target-isolation.ts
    - src/app/api/cron/zombie-recovery/route.ts
    - src/lib/logger.ts
    - vitest.config.ts
  </read_first>
  <action>
**Create test files first (RED phase):**

**1. Create `src/lib/action-worker/__tests__/claim.test.ts`:**
- Test: claimAction returns `{ claimed: true, action: {...} }` when RPC returns a row
- Test: claimAction returns `{ claimed: false }` when RPC returns empty array (already claimed)
- Test: claimAction returns `{ claimed: false, error: "..." }` when RPC returns error
- Mock Supabase client with `vi.fn()` for `rpc("claim_action", ...)`

**2. Create `src/lib/action-worker/__tests__/limits.test.ts`:**
- Test: checkAndIncrementLimit returns true when RPC returns true
- Test: checkAndIncrementLimit returns false when RPC returns false (limit reached)
- Test: checkAndIncrementLimit returns false on RPC error
- Test: getDailyUsage returns zeros when no row exists
- Mock Supabase client

**3. Create `src/lib/action-worker/__tests__/expiry.test.ts`:**
- Test: expiry logic marks actions older than 12h as expired
- Test: expiry logic does not touch actions younger than 12h
- Test: expiry logic resets associated prospects to detected status
- Mock Supabase client

Run all tests -- must fail (RED).

**4. Create `src/lib/action-worker/claim.ts`:**

```typescript
import type { SupabaseClient } from "@supabase/supabase-js"
import type { Action } from "@/features/actions/lib/types"

export async function claimAction(supabase: SupabaseClient, actionId: string): Promise<{
  claimed: boolean
  action: Action | null
  error: string | null
}> {
  const { data, error } = await supabase.rpc("claim_action", {
    p_action_id: actionId,
  })
  if (error) return { claimed: false, action: null, error: error.message }
  if (!data || data.length === 0) return { claimed: false, action: null, error: "Already claimed or not approved" }
  return { claimed: true, action: data[0] as Action, error: null }
}
```

**5. Create `src/lib/action-worker/limits.ts`:**

```typescript
import type { SupabaseClient } from "@supabase/supabase-js"

export async function checkAndIncrementLimit(
  supabase: SupabaseClient,
  accountId: string,
  actionType: string,
): Promise<boolean> {
  const { data, error } = await supabase.rpc("check_and_increment_limit", {
    p_account_id: accountId,
    p_action_type: actionType,
  })
  if (error) return false
  return data === true
}

export async function getDailyUsage(
  supabase: SupabaseClient,
  accountId: string,
): Promise<{ dm_count: number; engage_count: number; reply_count: number }> {
  const { data } = await supabase
    .from("action_counts")
    .select("dm_count, engage_count, reply_count")
    .eq("account_id", accountId)
    .eq("date", new Date().toISOString().split("T")[0])
    .single()
  return data ?? { dm_count: 0, engage_count: 0, reply_count: 0 }
}
```

Run claim and limits tests -- must pass (GREEN).

**6. Create `src/lib/action-worker/worker.ts`:**

CRITICAL: This file MUST import and call anti-ban modules from 03-04. The worker pipeline must integrate delays, noise, target isolation, warmup gating, and active hours checking.

```typescript
import { createClient } from "@supabase/supabase-js"
import { connectToProfile, disconnectProfile } from "@/lib/gologin/adapter"
import { executeCUAction } from "@/lib/computer-use/executor"
import { uploadScreenshot } from "@/lib/computer-use/screenshot"
import { getRedditDMPrompt } from "@/lib/computer-use/actions/reddit-dm"
import { getRedditLikePrompt, getRedditFollowPrompt } from "@/lib/computer-use/actions/reddit-engage"
import { claimAction } from "./claim"
import { checkAndIncrementLimit } from "./limits"
import { checkAndAssignTarget } from "./target-isolation"
import { randomDelay, sleep, isWithinActiveHours } from "./delays"
import { shouldInjectNoise, generateNoiseActions } from "./noise"
import { getWarmupState } from "@/features/accounts/lib/types"
import { logger } from "@/lib/logger"
import type { SocialAccount } from "@/features/accounts/lib/types"

export async function executeAction(actionId: string, correlationId: string): Promise<{ success: boolean; error?: string }> {
  const supabase = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
  )

  // 1. Claim action (FOR UPDATE SKIP LOCKED)
  const claim = await claimAction(supabase, actionId)
  if (!claim.claimed) return { success: false, error: claim.error ?? "Claim failed" }
  const action = claim.action!

  // 2. Get social account (typed, not `any`)
  const { data: account } = await supabase
    .from("social_accounts")
    .select("*")
    .eq("id", action.account_id)
    .single<SocialAccount>()
  if (!account?.gologin_profile_id) {
    await updateActionStatus(supabase, actionId, "failed", "No GoLogin profile")
    return { success: false, error: "No GoLogin profile" }
  }

  // 3. ANTI-BAN: Check active hours (ABAN-05)
  if (!isWithinActiveHours(account.timezone, account.active_hours_start, account.active_hours_end)) {
    await updateActionStatus(supabase, actionId, "approved", null) // Re-queue for later
    logger.info("Action deferred: outside active hours", { actionId, correlationId, timezone: account.timezone })
    return { success: false, error: "Outside active hours -- re-queued" }
  }

  // 4. ANTI-BAN: Check warmup gate (ABAN-02)
  const warmup = getWarmupState(account.warmup_day, account.warmup_completed_at)
  if (!warmup.allowedActions.includes(action.action_type as "dm" | "like" | "follow" | "public_reply")) {
    await updateActionStatus(supabase, actionId, "failed", `Warmup day ${warmup.day}: ${action.action_type} not yet allowed`)
    return { success: false, error: `Warmup gate: ${action.action_type} not allowed on day ${warmup.day}` }
  }

  // 5. ANTI-BAN: Target isolation (ABAN-06) -- check before executing
  const target = await checkAndAssignTarget(supabase, action.prospect_id, action.account_id!)
  if (!target.allowed) {
    await updateActionStatus(supabase, actionId, "failed", target.error ?? "Target isolation blocked")
    return { success: false, error: target.error ?? "Target isolation blocked" }
  }

  // 6. Check daily limits
  const withinLimits = await checkAndIncrementLimit(supabase, action.account_id!, action.action_type)
  if (!withinLimits) {
    await updateActionStatus(supabase, actionId, "failed", "Daily limit reached")
    return { success: false, error: "Daily limit reached" }
  }

  // 7. ANTI-BAN: Random delay before execution (ABAN-03)
  const delay = randomDelay()
  logger.info("Anti-ban delay", { actionId, correlationId, delaySeconds: delay })
  await sleep(delay)

  // 8. Connect GoLogin profile
  let connection
  try {
    connection = await connectToProfile(account.gologin_profile_id)
  } catch (err) {
    await updateActionStatus(supabase, actionId, "failed", `GoLogin connection failed: ${err}`)
    return { success: false, error: "GoLogin connection failed" }
  }

  try {
    // 9. Navigate to Reddit first
    await connection.page.goto("https://www.reddit.com", { waitUntil: "domcontentloaded", timeout: 30000 })

    // 10. ANTI-BAN: Inject behavioral noise before real action (ABAN-04)
    if (shouldInjectNoise()) {
      const noisePrompts = generateNoiseActions()
      for (const noisePrompt of noisePrompts) {
        await executeCUAction(connection.page, noisePrompt)
        await sleep(randomDelay(30, 15, 10)) // Shorter delay between noise actions
      }
    }

    // 11. Build CU prompt based on action type
    let prompt: string
    const prospect = await supabase.from("prospects").select("handle").eq("id", action.prospect_id).single()
    const handle = prospect.data?.handle ?? ""

    if (action.action_type === "dm" || action.action_type === "followup_dm") {
      prompt = getRedditDMPrompt(handle, action.final_content ?? action.drafted_content ?? "")
    } else if (action.action_type === "like") {
      const { data: signal } = await supabase.from("intent_signals").select("post_url").eq("id", action.prospect_id).maybeSingle()
      prompt = getRedditLikePrompt(signal?.post_url ?? "")
    } else if (action.action_type === "follow") {
      prompt = getRedditFollowPrompt(handle)
    } else {
      prompt = `Perform a public reply action for ${handle}`
    }

    // 12. Execute CU action
    const result = await executeCUAction(connection.page, prompt)

    // 13. Upload final screenshot
    if (result.screenshots.length > 0) {
      const lastScreenshot = result.screenshots[result.screenshots.length - 1]
      const url = await uploadScreenshot(actionId, lastScreenshot, result.steps)
      if (url) {
        await supabase.from("actions").update({ screenshot_url: url }).eq("id", actionId)
      }
    }

    // 14. Update status
    if (result.success) {
      await updateActionStatus(supabase, actionId, "completed", null)
      if (action.action_type === "dm") {
        await supabase.from("prospects").update({ pipeline_status: "contacted" }).eq("id", action.prospect_id)
      } else if (action.action_type === "like" || action.action_type === "follow") {
        await supabase.from("prospects").update({ pipeline_status: "engaged" }).eq("id", action.prospect_id)
      }
    } else {
      await updateActionStatus(supabase, actionId, "failed", result.error ?? "CU action failed")
    }

    // 15. Log to job_logs
    await supabase.from("job_logs").insert({
      job_type: "action",
      status: result.success ? "completed" : "failed",
      duration_ms: 0,
      details: { actionId, steps: result.steps, error: result.error },
      correlation_id: correlationId,
    })

    return { success: result.success, error: result.error }
  } finally {
    await disconnectProfile(connection.browser)
  }
}

async function updateActionStatus(
  supabase: ReturnType<typeof createClient>,
  actionId: string,
  status: string,
  error: string | null,
) {
  await supabase.from("actions").update({ status, error, executed_at: new Date().toISOString() }).eq("id", actionId)
}
```

NOTE on typing: The `updateActionStatus` helper uses `ReturnType<typeof createClient>` instead of `any` to satisfy CLAUDE.md strict mode. The `account` query uses `.single<SocialAccount>()` for typed results.

**7. Create `src/app/api/webhooks/actions/route.ts`:**

```typescript
import { NextRequest, NextResponse } from "next/server"
import { executeAction } from "@/lib/action-worker/worker"
import { logger } from "@/lib/logger"

export const maxDuration = 300 // 5 minutes for Vercel Pro Fluid Compute

export async function POST(req: NextRequest) {
  // 1. Verify webhook secret
  const auth = req.headers.get("authorization")
  if (auth !== `Bearer ${process.env.WEBHOOK_SECRET}`) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 })
  }

  const correlationId = crypto.randomUUID()
  const payload = await req.json()
  const { type, record: newRecord, old_record: oldRecord } = payload

  // 2. Only process status changes to 'approved'
  if (type !== "UPDATE" || newRecord?.status !== "approved") {
    return NextResponse.json({ skipped: true })
  }
  if (oldRecord?.status === "approved") {
    return NextResponse.json({ skipped: true })
  }

  logger.info("Action webhook received", { actionId: newRecord.id, correlationId })

  // 3. Execute action
  const result = await executeAction(newRecord.id, correlationId)

  await logger.flush()
  return NextResponse.json(result)
}
```

**8. Create `src/app/api/cron/expire-actions/route.ts`:**

Follow the cron route pattern from `src/app/api/cron/zombie-recovery/route.ts`:
- Auth: Bearer CRON_SECRET header check
- Setup: correlation ID + start timestamp
- Operation: service role Supabase client, update actions where `status = 'pending_approval'` AND `created_at < now() - interval '12 hours'` to `status = 'expired'`
- Also update associated prospects back to `pipeline_status = 'detected'`
- Log count of expired actions
- Flush logger before returning

Add to `vercel.json` cron schedule: `{ "path": "/api/cron/expire-actions", "schedule": "0 * * * *" }` (every hour).

Run all test files -- claim, limits, expiry, stuck-detection tests must pass (GREEN).
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/action-worker/__tests__/ src/lib/computer-use/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/action-worker/worker.ts exports `executeAction`
    - worker.ts imports `checkAndAssignTarget` from `./target-isolation`
    - worker.ts imports `randomDelay`, `sleep`, `isWithinActiveHours` from `./delays`
    - worker.ts imports `shouldInjectNoise`, `generateNoiseActions` from `./noise`
    - worker.ts imports `getWarmupState` from `@/features/accounts/lib/types`
    - worker.ts calls `isWithinActiveHours` BEFORE connecting GoLogin
    - worker.ts calls `getWarmupState` and checks `allowedActions` BEFORE executing
    - worker.ts calls `checkAndAssignTarget` BEFORE executing
    - worker.ts calls `randomDelay` + `sleep` BEFORE connecting GoLogin
    - worker.ts calls `shouldInjectNoise` + `generateNoiseActions` BEFORE real CU action
    - worker.ts does NOT use `any` type (uses `ReturnType<typeof createClient>` and `SocialAccount`)
    - src/lib/action-worker/claim.ts exports `claimAction` with typed return
    - src/lib/action-worker/limits.ts exports `checkAndIncrementLimit` and `getDailyUsage`
    - src/app/api/webhooks/actions/route.ts exports `POST` with `maxDuration = 300`
    - src/app/api/cron/expire-actions/route.ts exports `GET` with CRON_SECRET auth
    - vercel.json contains `/api/cron/expire-actions` schedule
    - All 4 test files exist and pass (claim, limits, expiry, stuck-detection)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Complete action execution pipeline with anti-ban modules wired in: target isolation -> warmup gate -> active hours -> delay -> noise -> execute. Tests for claim, limits, expiry, stuck-detection all passing.</done>
</task>

</tasks>

<verification>
- `grep "executeCUAction" src/lib/computer-use/executor.ts` returns match
- `grep "checkAndAssignTarget" src/lib/action-worker/worker.ts` returns match
- `grep "isWithinActiveHours" src/lib/action-worker/worker.ts` returns match
- `grep "getWarmupState" src/lib/action-worker/worker.ts` returns match
- `grep "shouldInjectNoise" src/lib/action-worker/worker.ts` returns match
- `grep "randomDelay" src/lib/action-worker/worker.ts` returns match
- `grep "createSignedUrl" src/lib/computer-use/screenshot.ts` returns match
- `grep "maxDuration = 300" src/app/api/webhooks/actions/route.ts` returns match
- `grep "expire-actions" vercel.json` returns match
- `pnpm vitest run src/lib/action-worker/__tests__/ src/lib/computer-use/__tests__/ --reporter=verbose` exits 0
- `pnpm typecheck` exits 0
</verification>

<success_criteria>
- Full pipeline: webhook -> claim -> target isolation -> warmup gate -> active hours -> limits -> delay -> noise -> GoLogin connect -> CU execute -> screenshot upload -> status update -> job log
- Anti-ban modules are imported and called in worker.ts (not dead code)
- Haiku CU capped at 15 steps with 3-identical-screenshot stuck detection
- Webhook handler validates secret and only processes approved status transitions
- Expiry cron runs hourly and expires 12h-old pending_approval actions
- No `any` types used -- all Supabase clients and query results are typed
- Screenshot URLs use createSignedUrl (private bucket, 7-day expiry)
- 4 test files created and passing: claim, limits, expiry, stuck-detection
</success_criteria>

<output>
After completion, create `.planning/phases/03-action-engine/03-03-SUMMARY.md`
</output>
