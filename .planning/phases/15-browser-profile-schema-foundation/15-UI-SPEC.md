---
phase: 15
slug: browser-profile-schema-foundation
status: draft
shadcn_initialized: true
preset: radix-nova
created: 2026-04-27
---

# Phase 15 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## UI Surface Assessment

**This phase has no new user-facing UI surface.**

Phase 15 is a schema migration + code refactor phase. The explicit out-of-scope declaration in CONTEXT.md reads: "Any UX changes (no new 'Dodaj konto' flow yet — allocator lands in Phase 17)". The allocator UI, new account connection flow, and proxy/profile management UX are all deferred.

The one file that touches a component (`account-card.tsx`) requires only a **type-level refactor** — replacing the `account.gologin_profile_id` prop reference with `account.browser_profile_id`. No visual output changes. The rendered HTML is identical before and after.

Downstream agents (planner, executor, checker) should treat this as a zero-UI-delta phase. The contract sections below are pre-populated from the existing design system for completeness and checker compliance, but no new pixels ship in Phase 15.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json (detected) |
| Preset | radix-nova | components.json |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | Phosphor | components.json `"iconLibrary": "phosphor"` |
| Font | System sans (var(--font-sans)) | globals.css |

**Note:** Lucide icons are also present in account-card.tsx (LogIn, Trash2, CheckCircle2, MessageSquare). Both Phosphor and Lucide coexist in the project. Phase 15 introduces no new icon usage.

---

## Spacing Scale

Declared values (must be multiples of 4) — inherited from existing system, no changes in Phase 15:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

---

## Typography

Inherited from existing design system (radix-nova preset + stone base). No new typographic elements introduced in Phase 15.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 14px | 500 | 1.4 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 700 | 1.2 |

Source: inferred from existing account-card.tsx usage (`text-xl font-semibold`, `text-sm text-muted-foreground`) and radix-nova defaults.

---

## Color

Inherited from existing globals.css CSS variables (oklch, radix-nova / stone base). No new color usage in Phase 15.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | oklch(1 0 0) / dark surface | Background, main content area |
| Secondary (30%) | oklch(0.985 0.001 106.423) | Sidebar, card surfaces |
| Accent (10%) | oklch(0.457 0.24 277.023) — violet/purple primary | Primary buttons, active nav items, focus rings |
| Destructive | oklch(0.577 0.245 27.325) — red | Delete account confirmation dialog only |

Accent reserved for: primary action buttons, active sidebar navigation, focus ring on interactive elements.

Platform color tokens already in use (not modified by Phase 15):
- Reddit badge: `#FF4500`
- LinkedIn badge: `#0A66C2`

---

## Copywriting Contract

Phase 15 introduces no new copy. The `account-card.tsx` refactor touches no user-visible strings.

The existing delete confirmation copy in account-card.tsx line 198 reads:
> "Removes {username} and its browser profile. Pending actions for this account will stop. This cannot be undone."

This string already uses "browser profile" language (committed prior to Phase 15). No update needed.

| Element | Copy | Status |
|---------|------|--------|
| Primary CTA | n/a — no new CTA in Phase 15 | Not applicable |
| Empty state heading | n/a — no new empty state | Not applicable |
| Empty state body | n/a | Not applicable |
| Error state | n/a — no new error surfaces | Not applicable |
| Destructive confirmation | Existing: "Delete {platform} account?" / "Removes {username} and its browser profile..." | Pre-existing, no change |

---

## Component Inventory

### Modified (type refactor only — zero visual delta)

| File | Change | Visual Impact |
|------|--------|---------------|
| `src/features/accounts/components/account-card.tsx` | `account.gologin_profile_id` → `account.browser_profile_id` in `onReconnect` call | None |
| `src/features/accounts/lib/types.ts` | `SocialAccount`: drop `gologin_profile_id`, drop `proxy_id`, add `browser_profile_id: string \| null` | None |

### New (non-UI)

| File | Type | Notes |
|------|------|-------|
| `src/features/browser-profiles/lib/get-browser-profile.ts` | Library helper | No component, no UI |

### No UI registry additions

No shadcn components are added or removed in Phase 15. No blocks fetched.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | None — no new components installed | not required |

No third-party registries declared. Registry vetting gate not applicable.

---

## Interaction Contracts

No new interactions are introduced. The account card's "Delete" flow remains unchanged. No new modals, drawers, toasts, or loading states are added.

Loading/pending states in account-card.tsx (`isDeleting` transition) are pre-existing and unchanged.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — no new copy; existing delete confirmation pre-correct
- [ ] Dimension 2 Visuals: PASS — no new screens, no visual delta
- [ ] Dimension 3 Color: PASS — no new color tokens; existing palette inherited
- [ ] Dimension 4 Typography: PASS — no new typographic elements
- [ ] Dimension 5 Spacing: PASS — no new layout elements
- [ ] Dimension 6 Registry Safety: PASS — no third-party registry usage

**Approval:** pending

---

*Phase: 15-browser-profile-schema-foundation*
*UI-SPEC generated: 2026-04-27*
*Verdict: Zero-UI-delta phase. Schema and refactor only. All existing design tokens inherited.*
