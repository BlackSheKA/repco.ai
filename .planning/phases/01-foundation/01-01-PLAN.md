---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - src/components/providers/theme-provider.tsx
  - tailwind.config.ts
  - components.json
  - .env.local
  - .env.example
  - vercel.json
  - next.config.ts
autonomous: true
requirements:
  - OBSV-03
must_haves:
  truths:
    - "Next.js 15 dev server starts without errors"
    - "shadcn/ui components render with brand colors and fonts"
    - "Supabase client utilities export createClient for both browser and server"
    - "Three Google Fonts (Instrument Serif, Inter, JetBrains Mono) load on page"
    - "Theme toggle cycles system -> light -> dark -> system"
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client"
      exports: ["createClient"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client with async cookies"
      exports: ["createClient"]
    - path: "src/lib/supabase/middleware.ts"
      provides: "Middleware auth helper"
      exports: ["updateSession"]
    - path: "src/components/providers/theme-provider.tsx"
      provides: "next-themes provider wrapper"
      exports: ["ThemeProvider"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, ThemeProvider, Sonner"
      contains: "Instrument_Serif"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/theme-provider.tsx"
      via: "import ThemeProvider"
      pattern: "ThemeProvider"
    - from: "src/lib/supabase/server.ts"
      to: "@supabase/ssr"
      via: "createServerClient import"
      pattern: "createServerClient"
---

<objective>
Scaffold the Next.js 15 project with shadcn/ui (preset b3QwALGmg), brand theming (fonts, colors), Supabase client utilities, and foundational configuration files.

Purpose: Establish the project skeleton that all subsequent plans build on -- branded, themed, with working Supabase client helpers.
Output: A running Next.js 15 dev server with brand fonts, shadcn components, theme toggle, and Supabase client utilities ready for auth integration.
</objective>

<execution_context>
@C:/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project with shadcn/ui preset and install all Phase 1 dependencies</name>
  <files>package.json, pnpm-lock.yaml, components.json, tailwind.config.ts, src/app/globals.css, next.config.ts, .env.local, .env.example, vercel.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (standard stack versions, install commands)
    - .planning/phases/01-foundation/01-UI-SPEC.md (design system tokens, color values, spacing scale)
    - .planning/phases/01-foundation/01-CONTEXT.md (locked decisions on shadcn preset, pnpm)
  </read_first>
  <action>
1. Initialize the project using the locked shadcn preset command:
   ```
   pnpm dlx shadcn@latest init --preset b3QwALGmg --template next
   ```

2. Install core dependencies:
   ```
   pnpm add @supabase/supabase-js@^2.103.3 @supabase/ssr@^0.10.2 @sentry/nextjs@^10.49.0 @axiomhq/js@^1.6.0 @axiomhq/logging@^0.2.0 @axiomhq/nextjs@^0.2.0 @axiomhq/react@^0.2.0 next-themes@^0.4.6 lucide-react@^1.8.0 sonner@^2.0.7
   ```

3. Install dev dependencies:
   ```
   pnpm add -D supabase
   ```

4. Add shadcn/ui components needed for Phase 1:
   ```
   pnpm dlx shadcn@latest add button input separator avatar sonner dropdown-menu alert-dialog
   ```

5. After shadcn init, inspect the generated tailwind.config.ts and globals.css. Override/extend the CSS variables to match the brand spec:

   **Colors (CSS custom properties in globals.css):**
   - Light mode dominant background: `#FFFFFF` (hsl 0 0% 100%)
   - Light mode secondary surface: `#F4F4F5` (hsl 240 5% 96%)
   - Dark mode dominant background: `#09090B` (hsl 240 10% 4%)
   - Dark mode secondary surface: `#18181B` (hsl 240 6% 10%)
   - Accent: `#E8500A` (hsl 19 93% 47%)
   - Destructive: `#DC2626` (hsl 0 84% 50%)
   - Muted text light: `#71717A` (hsl 240 4% 46%)
   - Muted text dark: `#A1A1AA` (hsl 240 5% 65%)
   - Border light: `#E4E4E7` (hsl 240 6% 90%)
   - Border dark: `#27272A` (hsl 240 4% 16%)

   Map these to shadcn CSS variable names: `--background`, `--foreground`, `--card`, `--primary`, `--destructive`, `--muted`, `--muted-foreground`, `--border`, `--accent`, `--accent-foreground`, etc.

6. Create `.env.local` with placeholder values:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder
   SUPABASE_SERVICE_ROLE_KEY=placeholder
   NEXT_PUBLIC_SITE_URL=http://localhost:3000
   SENTRY_DSN=
   SENTRY_AUTH_TOKEN=
   SENTRY_ORG=
   SENTRY_PROJECT=
   AXIOM_TOKEN=
   AXIOM_DATASET=repco
   CRON_SECRET=local-dev-secret
   ```

7. Create `.env.example` with the same keys but empty values (no placeholders that look like real values).

8. Create `vercel.json`:
   ```json
   {
     "$schema": "https://openapi.vercel.sh/vercel.json",
     "crons": [
       {
         "path": "/api/cron/zombie-recovery",
         "schedule": "*/5 * * * *"
       }
     ]
   }
   ```

9. Ensure `next.config.ts` has no conflicting settings. Leave it minimal -- Sentry wrapping comes in Plan 04.
  </action>
  <verify>
    <automated>cd C:/Users/kamil/Code/repco.ai && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@supabase/supabase-js"
    - package.json contains "@supabase/ssr"
    - package.json contains "@sentry/nextjs"
    - package.json contains "@axiomhq/nextjs"
    - package.json contains "next-themes"
    - package.json contains "sonner"
    - components.json exists and contains "shadcn"
    - .env.example contains "NEXT_PUBLIC_SUPABASE_URL="
    - .env.example contains "CRON_SECRET="
    - vercel.json contains "zombie-recovery"
    - src/app/globals.css contains "#E8500A" or the HSL equivalent for accent
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Next.js 15 project builds successfully with all Phase 1 dependencies installed, shadcn/ui initialized with preset, brand colors in CSS variables, env files created, vercel.json with cron config.</done>
</task>

<task type="auto">
  <name>Task 2: Configure brand fonts, root layout, theme provider, and Supabase client utilities</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/components/providers/theme-provider.tsx, src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/middleware.ts</files>
  <read_first>
    - src/app/layout.tsx (current state after shadcn init)
    - src/app/globals.css (current state after Task 1 theming)
    - .planning/phases/01-foundation/01-RESEARCH.md (Supabase SSR patterns, async cookies)
    - .planning/phases/01-foundation/01-UI-SPEC.md (typography spec, font assignments)
  </read_first>
  <action>
1. **Root layout (src/app/layout.tsx):**
   - Import three Google Fonts from `next/font/google`:
     - `Instrument_Serif` with weight "400", subsets ["latin"], variable `--font-heading`
     - `Inter` with subsets ["latin"], variable `--font-body`
     - `JetBrains_Mono` with subsets ["latin"], variable `--font-mono`
   - Apply font variables to `<html>` className: `${instrumentSerif.variable} ${inter.variable} ${jetbrainsMono.variable}`
   - Set Inter as the default body font via Tailwind (body class `font-body`)
   - Wrap children with `<ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>`
   - Add `<Toaster />` from sonner (shadcn Sonner component) as last child inside body
   - Set metadata: title "repco", description "Your AI sales rep that never sleeps."

2. **Theme provider (src/components/providers/theme-provider.tsx):**
   ```tsx
   "use client";
   import { ThemeProvider as NextThemesProvider } from "next-themes";
   import { type ComponentProps } from "react";
   
   export function ThemeProvider({ children, ...props }: ComponentProps<typeof NextThemesProvider>) {
     return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
   }
   ```

3. **Tailwind font config:**
   In `tailwind.config.ts`, extend fontFamily:
   ```
   fontFamily: {
     heading: ["var(--font-heading)", "Georgia", "serif"],
     body: ["var(--font-body)", "system-ui", "sans-serif"],
     mono: ["var(--font-mono)", "monospace"],
   }
   ```

4. **Supabase browser client (src/lib/supabase/client.ts):**
   ```typescript
   import { createBrowserClient } from "@supabase/ssr";

   export function createClient() {
     return createBrowserClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
     );
   }
   ```

5. **Supabase server client (src/lib/supabase/server.ts):**
   ```typescript
   import { createServerClient } from "@supabase/ssr";
   import { cookies } from "next/headers";

   export async function createClient() {
     const cookieStore = await cookies();
     return createServerClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() { return cookieStore.getAll(); },
           setAll(cookiesToSet) {
             cookiesToSet.forEach(({ name, value, options }) =>
               cookieStore.set(name, value, options)
             );
           },
         },
       }
     );
   }
   ```
   CRITICAL: `cookies()` MUST be awaited (Next.js 15 breaking change).

6. **Supabase middleware helper (src/lib/supabase/middleware.ts):**
   ```typescript
   import { createServerClient } from "@supabase/ssr";
   import { NextResponse, type NextRequest } from "next/server";

   export async function updateSession(request: NextRequest) {
     let response = NextResponse.next({ request });
     const supabase = createServerClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() { return request.cookies.getAll(); },
           setAll(cookiesToSet) {
             cookiesToSet.forEach(({ name, value }) =>
               request.cookies.set(name, value)
             );
             response = NextResponse.next({ request });
             cookiesToSet.forEach(({ name, value, options }) =>
               response.cookies.set(name, value, options)
             );
           },
         },
       }
     );
     await supabase.auth.getUser();
     return { response, supabase };
   }
   ```

7. **Placeholder page (src/app/page.tsx):** Simple centered text "repco" in font-heading, 40px, to verify fonts load. This is temporary -- replaced in Plan 03 with the app shell.
  </action>
  <verify>
    <automated>cd C:/Users/kamil/Code/repco.ai && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains "Instrument_Serif"
    - src/app/layout.tsx contains "Inter"
    - src/app/layout.tsx contains "JetBrains_Mono"
    - src/app/layout.tsx contains "ThemeProvider"
    - src/app/layout.tsx contains "Toaster"
    - src/components/providers/theme-provider.tsx contains "NextThemesProvider"
    - src/lib/supabase/client.ts contains "createBrowserClient"
    - src/lib/supabase/server.ts contains "createServerClient"
    - src/lib/supabase/server.ts contains "await cookies()"
    - src/lib/supabase/middleware.ts contains "updateSession"
    - src/lib/supabase/middleware.ts contains "supabase.auth.getUser()"
    - tailwind.config.ts contains "font-heading" or fontFamily heading definition
    - `pnpm build` exits with code 0
  </acceptance_criteria>
  <done>Root layout loads three branded fonts, wraps app in ThemeProvider + Toaster, Supabase client/server/middleware utilities are ready for auth integration, build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with exit code 0
- `pnpm dev` starts without errors on localhost:3000
- Page renders with Instrument Serif heading font
- .env.example lists all required environment variables
- src/lib/supabase/ contains client.ts, server.ts, middleware.ts with correct exports
</verification>

<success_criteria>
- Next.js 15 project scaffolded with shadcn/ui preset b3QwALGmg
- All Phase 1 dependencies installed (Supabase, Sentry, Axiom, next-themes, sonner, lucide-react)
- Brand colors configured as CSS variables in globals.css
- Three Google Fonts loaded and mapped to Tailwind font families
- Supabase browser + server + middleware client utilities created
- ThemeProvider wraps the app with system/light/dark support
- vercel.json defines zombie-recovery cron schedule
- .env.example documents all required env vars
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
