---
phase: 01-foundation
plan: 01
subsystem: infra
tags: [nextjs, shadcn, tailwind-v4, supabase-ssr, next-themes, sonner, google-fonts]

requires: []
provides:
  - "Next.js project skeleton with shadcn/ui (radix-nova style, Tailwind v4)"
  - "Brand theming: Inter / Geist / Geist Mono fonts + #4338CA indigo primary palette"
  - "Supabase browser/server/middleware client utilities"
  - "ThemeProvider with system/light/dark support and hotkey toggle"
  - "Sonner toast notifications via shadcn Toaster component"
  - "vercel.json with zombie-recovery cron schedule"
  - "Environment variable template (.env.example)"
affects: [01-02, 01-03, 01-04, 02-reddit-monitoring]

tech-stack:
  added: [next@16.1.7, tailwindcss@4.2, @supabase/supabase-js, @supabase/ssr, @sentry/nextjs, @axiomhq/nextjs, next-themes, lucide-react, sonner, shadcn@4.3]
  patterns: [tailwind-v4-css-config, supabase-ssr-cookies, next-font-google-variables]

key-files:
  created:
    - src/app/layout.tsx
    - src/app/globals.css
    - src/components/providers/theme-provider.tsx
    - src/lib/supabase/client.ts
    - src/lib/supabase/server.ts
    - src/lib/supabase/middleware.ts
    - .env.example
    - vercel.json
    - components.json
  modified: []

key-decisions:
  - "Accepted Next.js 16 from shadcn preset instead of downgrading to 15 -- preset command is locked"
  - "Used Tailwind v4 CSS-based config instead of tailwind.config.ts -- generated by preset"
  - "Kept phosphor icons from preset alongside lucide-react -- both available"
  - "Used oklch color values for brand palette to match preset convention"
  - "Restructured to src/ directory layout for feature-grouped organization"

patterns-established:
  - "Tailwind v4 @theme inline block for design tokens (colors, fonts, radii)"
  - "Supabase SSR pattern: async cookies() in server client, cookie relay in middleware"
  - "Font variable strategy: --font-sans (Inter), --font-geist-sans (Geist), --font-geist-mono (Geist Mono)"
  - "Brand color mapping: primary = indigo #4338CA, warm stone palette for surfaces"

requirements-completed: [OBSV-03]

duration: 8min
completed: 2026-04-17
---

# Phase 1 Plan 1: Project Scaffold Summary

**Next.js 16 + shadcn/ui (radix-nova) project with brand fonts (Inter/Geist/Geist Mono), #4338CA indigo primary palette, Supabase SSR client utilities, and ThemeProvider**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-17T07:49:58Z
- **Completed:** 2026-04-17T07:58:17Z
- **Tasks:** 2/2
- **Files modified:** 33

## Accomplishments

- Scaffolded Next.js project using locked shadcn preset b3QwALGmg with Tailwind v4 and radix-nova style
- Configured brand color palette (#4338CA indigo primary, warm stone neutrals) as oklch CSS variables for light/dark modes
- Loaded three fonts (Inter, Geist, Geist Mono) mapped to Tailwind font families
- Created Supabase browser, server (async cookies), and middleware client utilities
- Set up ThemeProvider with system/light/dark support plus keyboard hotkey toggle

## Task Commits

Each task was committed atomically:

1. **Task 1: Initialize Next.js 15 project with shadcn/ui preset and install all Phase 1 dependencies** - `8bba847` (feat)
2. **Task 2: Configure brand fonts, root layout, theme provider, and Supabase client utilities** - `424a359` (feat)

## Files Created/Modified

- `package.json` - Project manifest with all Phase 1 dependencies
- `components.json` - shadcn/ui configuration (radix-nova style, src/ paths)
- `src/app/globals.css` - Tailwind v4 theme with brand colors (indigo primary, stone palette) in oklch
- `src/app/layout.tsx` - Root layout with 3 brand fonts (Inter, Geist, Geist Mono), ThemeProvider, Toaster
- `src/app/page.tsx` - Placeholder with centered "repco" in Inter
- `src/components/providers/theme-provider.tsx` - next-themes wrapper with hotkey
- `src/components/ui/button.tsx` - shadcn Button component
- `src/components/ui/input.tsx` - shadcn Input component
- `src/components/ui/separator.tsx` - shadcn Separator component
- `src/components/ui/avatar.tsx` - shadcn Avatar component
- `src/components/ui/sonner.tsx` - shadcn Sonner (toast) component
- `src/components/ui/dropdown-menu.tsx` - shadcn Dropdown Menu component
- `src/components/ui/alert-dialog.tsx` - shadcn Alert Dialog component
- `src/lib/supabase/client.ts` - Browser Supabase client (createBrowserClient)
- `src/lib/supabase/server.ts` - Server Supabase client (async cookies, createServerClient)
- `src/lib/supabase/middleware.ts` - Middleware auth helper (updateSession)
- `src/lib/utils.ts` - Tailwind merge utility (cn)
- `.env.example` - Environment variable template (11 vars)
- `.env.local` - Local dev environment with placeholders
- `vercel.json` - Cron configuration (zombie-recovery every 5 min)
- `next.config.ts` - Minimal Next.js config (Sentry wrapping deferred to Plan 04)
- `tsconfig.json` - TypeScript config with @/* path alias to src/

## Decisions Made

1. **Next.js 16 instead of 15:** The locked shadcn preset command produced Next.js 16.1.7 instead of 15. Since the preset command was a user-locked decision, accepted the output rather than downgrading. The Supabase SSR patterns, async cookies, and App Router structure are identical.

2. **Tailwind v4 CSS-based config:** The preset generated Tailwind v4 which uses CSS `@theme` blocks instead of `tailwind.config.ts`. Brand colors and font families are registered in `globals.css` rather than a JS config file.

3. **oklch color values:** Matched the preset's convention of using oklch() color space instead of hsl() for better perceptual uniformity. The brand colors (#4338CA indigo, #DC2626, stone palette) were converted to oklch equivalents.

4. **src/ directory restructure:** Moved generated files from flat layout into `src/` directory to match the plan's feature-grouped folder structure. Updated tsconfig paths and components.json accordingly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adapted to Tailwind v4 from preset (expected Tailwind v3)**
- **Found during:** Task 1 (Project initialization)
- **Issue:** Plan specified `tailwind.config.ts` font family config, but preset generated Tailwind v4 which uses CSS-based configuration via `@theme` blocks
- **Fix:** Registered font families (`--font-sans`, `--font-geist-sans`, `--font-geist-mono`) in the `@theme inline` block of globals.css instead of tailwind.config.ts
- **Files modified:** src/app/globals.css
- **Verification:** `pnpm build` passes, font-sans/font-geist-sans/font-geist-mono classes resolve correctly
- **Committed in:** 8bba847, 424a359

**2. [Rule 3 - Blocking] Restructured from flat layout to src/ directory**
- **Found during:** Task 1 (Project initialization)
- **Issue:** shadcn preset created project in a subdirectory with flat layout (no src/), but plan requires src/ structure
- **Fix:** Moved app/, components/, lib/, hooks/ into src/, updated tsconfig.json paths and components.json CSS path
- **Files modified:** tsconfig.json, components.json
- **Verification:** Build passes with all imports resolving via @/* alias
- **Committed in:** 8bba847

**3. [Rule 3 - Blocking] next.config.mjs to next.config.ts conversion**
- **Found during:** Task 1 (Project initialization)
- **Issue:** Preset generated next.config.mjs but plan specifies next.config.ts
- **Fix:** Created next.config.ts with typed NextConfig, removed next.config.mjs
- **Files modified:** next.config.ts (created), next.config.mjs (deleted)
- **Committed in:** 8bba847

---

**Total deviations:** 3 auto-fixed (3 blocking issues from preset output differing from plan assumptions)
**Impact on plan:** All deviations were necessary adaptations to the preset's output. The preset command was locked, so we adapted the plan to the actual output. No scope creep.

## Issues Encountered

- shadcn init created project in a `repco/` subdirectory rather than in the current directory -- resolved by moving all files up to project root
- Axiom peer dependency warning (`@axiomhq/logging` expects `@axiomhq/js@1.4.0`, got `1.6.0`) -- non-blocking, will monitor

## User Setup Required

None - no external service configuration required for this plan. Environment variables in `.env.local` use placeholder values.

## Next Phase Readiness

- Project skeleton is ready for Plan 02 (database schema + migrations)
- Supabase client utilities are ready for Plan 02 (auth integration)
- shadcn/ui components are installed for Plan 03 (app shell + login page)
- Brand theming is complete -- all subsequent UI work uses the established palette and fonts
- Sentry wrapping deferred to Plan 04 as specified

## Self-Check: PASSED

- All 9 key files verified present on disk
- Commit 8bba847 (Task 1) verified in git log
- Commit 424a359 (Task 2) verified in git log
- `pnpm build` exits with code 0

---
*Phase: 01-foundation*
*Completed: 2026-04-17*
