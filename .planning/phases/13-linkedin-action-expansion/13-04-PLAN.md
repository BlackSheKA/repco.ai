---
phase: 13-linkedin-action-expansion
plan: 04
plan_number: 04
plan_name: linkedin-followup-dm-wiring
type: execute
wave: 5
depends_on: [13-03]
requirements: [LNKD-05]
files_modified:
  - src/lib/action-worker/worker.ts
  - src/lib/action-worker/expiry.ts
  - src/app/api/cron/check-replies/route.ts
  - src/app/api/cron/check-replies/__tests__/route.test.ts
  - src/lib/action-worker/__tests__/worker-linkedin-followup.test.ts
autonomous: true
must_haves:
  truths:
    - "Follow-up cron creates `followup_dm` rows for LinkedIn prospects (day 3/7/14 sequences) without any platform-specific filter"
    - "When worker dispatches a `followup_dm` for a LinkedIn account, it routes to `sendLinkedInDM` (shipped in 13-01) — not Haiku CU, not Reddit executor"
    - "Reddit `followup_dm` regression: Reddit prospects continue to flow through the Haiku CU executeCUAction path unchanged"
    - "`followup_dm` is NOT excluded from expiry logic for either platform (expiry.ts covers both `dm` and `followup_dm` uniformly)"
    - "Integration test: a LinkedIn DM backdated 3 days triggers cron → `followup_dm` row created → worker executes it through sendLinkedInDM mock → success=true"
  artifacts:
    - path: "src/lib/action-worker/__tests__/worker-linkedin-followup.test.ts"
      provides: "Integration test: LinkedIn followup_dm end-to-end (cron creates row, worker dispatches to DM executor)"
    - path: "src/app/api/cron/check-replies/__tests__/route.test.ts"
      provides: "Cron test: LinkedIn prospects eligible for followup, not filtered out by platform guard"
  key_links:
    - from: "src/lib/action-worker/worker.ts (linkedin dm arm from 13-01)"
      to: "sendLinkedInDM"
      via: "handles BOTH action_type='dm' AND 'followup_dm' (13-01 already wrote this)"
      pattern: "action_type === \"dm\" \\|\\| action.action_type === \"followup_dm\""
    - from: "src/app/api/cron/check-replies/route.ts"
      to: "actions table (followup_dm insert)"
      via: "platform-agnostic insert for prospects with accepted connection + no reply at day 3/7/14"
      pattern: "followup_dm"
    - from: "src/lib/action-worker/expiry.ts"
      to: "followup_dm action_type"
      via: "expiry does NOT skip followup_dm for LinkedIn"
      pattern: "followup_dm"
---

<objective>
Wire LinkedIn follow-up DM routing (LNKD-04). This plan is mostly VERIFICATION + TESTS + SMALL PATCHES — the actual DM execution path shipped in 13-01, which already handles both `dm` and `followup_dm` via the same `sendLinkedInDM` call (per 13-01's dispatch code).

Three concrete jobs:
1. Verify the day 3/7/14 follow-up cron creates `followup_dm` rows for LinkedIn prospects (not platform-gated). Patch if gated.
2. Verify `src/lib/action-worker/expiry.ts` treats `followup_dm` uniformly across platforms (LinkedIn not accidentally excluded). Patch if excluded.
3. Add integration test covering the LinkedIn followup_dm path end-to-end + cron-side regression test for Reddit.

Purpose: close the "LinkedIn DM loop" for accepted-connection prospects. Without this wiring, a LinkedIn prospect who accepts a Connect never gets the day-3/7/14 follow-up.

Output: patches to cron/expiry only if guards found; two new tests; SUMMARY documenting the no-change or small-patch verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-linkedin-action-expansion/13-CONTEXT.md
@.planning/phases/13-linkedin-action-expansion/13-RESEARCH.md
@.planning/phases/13-linkedin-action-expansion/13-VALIDATION.md
@.planning/phases/13-linkedin-action-expansion/13-01-PLAN.md
@src/lib/action-worker/worker.ts
@src/lib/action-worker/expiry.ts
@src/app/api/cron/check-replies/route.ts

<interfaces>
From 13-01 (ALREADY written by Plan 01):
```ts
// worker.ts inside account!.platform === "linkedin" branch
} else if (action.action_type === "dm" || action.action_type === "followup_dm") {
  const body = action.content ?? ""
  const dmResult = await sendLinkedInDM(page, prospect!.profile_url!, body)
  // ...
}
```
This means when `followup_dm` arrives for a LinkedIn account, dispatch is ALREADY correct. This plan's job is to verify the cron produces such rows and nothing upstream filters them out.

Follow-up cron behavior (assumed per 13-CONTEXT §In scope and 13-RESEARCH "cron already creates followup_dm rows platform-agnostically"):
- Scans prospects whose last `dm` or `connection_request` was N days ago (N ∈ {3, 7, 14})
- Creates `actions` row with `action_type='followup_dm'`, `content=<generated>`, `status='pending_approval'` or `'approved'` per existing policy
- Planner MUST locate the actual file during execution (likely `src/app/api/cron/check-replies/route.ts` per files_modified list, or a sibling cron like `src/app/api/cron/followup-dm/route.ts`). Use `grep -rn "followup_dm" src/app/api/cron/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + patch cron + expiry for LinkedIn followup_dm coverage</name>
  <read_first>
    - src/app/api/cron/check-replies/route.ts (full file)
    - Run `grep -rn "followup_dm" src/app/api/cron/` (identify the exact cron that INSERTS followup_dm rows — may not be check-replies)
    - Run `grep -rn "followup_dm" src/` (all call sites)
    - src/lib/action-worker/expiry.ts (full file)
    - src/lib/action-worker/worker.ts (confirm 13-01 shipped the `dm || followup_dm` dispatch inside LinkedIn branch — grep `action_type === "followup_dm"`)
    - .planning/phases/13-linkedin-action-expansion/13-CONTEXT.md §In scope (follow-up cron policy)
  </read_first>
  <action>
    **A. Audit step — no code yet.**

    Run the greps above and record findings in a top-of-task comment block:
    ```
    // AUDIT RESULTS (13-04 Task 1)
    // Follow-up cron file: <path>
    // Platform filter present: yes|no (line refs)
    // Expiry treatment of followup_dm: uniform|skipped-for-linkedin (line refs)
    // Worker dispatch for followup_dm on LinkedIn: wired via 13-01 at line <N>
    ```

    **B. Cron patch (ONLY IF platform filter found):**

    If the follow-up cron has a WHERE clause like `platform = 'reddit'` or `account.platform != 'linkedin'`, remove it. The cron must be platform-agnostic — worker dispatches by platform, not cron.

    If the cron uses a prospect query like `.eq("platform", "reddit")` to find candidates, broaden to `.in("platform", ["reddit", "linkedin"])` OR remove the `.eq` entirely. Preserve existing filters for `pipeline_status` (must be `contacted` or `connected` etc. — the eligibility criteria per Phase 4).

    Eligibility rule for LinkedIn follow-ups (per CONTEXT §In scope + §Non-1st-degree DM handling): only prospects whose `pipeline_status='connected'` (accepted the Connect request) qualify for followup_dm on LinkedIn. If the existing cron uses `pipeline_status IN ('contacted', 'replied')` (Reddit-centric — Reddit prospects go straight to `contacted` after first DM), extend to `pipeline_status IN ('contacted','connected','replied')`. Confirm exact semantic with Phase 4 SUMMARY before editing (grep `.planning/phases/04-*-SUMMARY.md` for "followup" and "pipeline_status").

    Cite: `// LNKD-04 per 13-CONTEXT.md §In scope — worker dispatches by platform; cron is platform-agnostic`.

    **C. Expiry patch (ONLY IF exclusion found):**

    In `src/lib/action-worker/expiry.ts`, search for `followup_dm` and any `platform` check. If there's logic like `if (actionType === 'followup_dm' && platform === 'linkedin') { skip }` → remove it. LinkedIn followup_dm must expire on the same rules as Reddit followup_dm.

    If no such exclusion exists, leave the file untouched and document the verification in the SUMMARY.

    **D. Worker dispatch sanity check (no code change expected):**

    Verify `grep -q 'action_type === "dm" || action.action_type === "followup_dm"' src/lib/action-worker/worker.ts` returns a match inside the LinkedIn branch. If not, 13-01 is incomplete — STOP and surface the gap (do not write the dispatch here; it's 13-01's responsibility).

    Acceptance: every patch must include a code-comment citing LNKD-05 and the specific CONTEXT/RESEARCH clause that motivated it.

    **E. Audit comment (MANDATORY regardless of patch outcome):**

    Append a single-line audit comment to EACH audited file (`src/app/api/cron/check-replies/route.ts`, `src/lib/action-worker/expiry.ts`) in the form:

    ```
    // AUDIT(13-04): no-change — LinkedIn followup eligible via platform-agnostic filter (verified 2026-04-23)
    ```

    OR if patched:

    ```
    // AUDIT(13-04): patched line NN — removed platform='reddit' exclusion; see 13-SUMMARY.md
    ```

    This comment is the observable proof the audit ran. Without it, the verify step fails.
  </action>
  <verify>
    <automated>grep -qE "AUDIT\(13-04\):" src/app/api/cron/check-replies/route.ts src/lib/action-worker/expiry.ts &amp;&amp; grep -q "followup_dm" src/lib/action-worker/worker.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    - Audit comment recorded in task notes (captures exact file paths + findings)
    - If patch needed in cron: platform filter removed / extended, grep confirms no `platform.*=.*'reddit'` exclusion for followup eligibility
    - If patch needed in expiry: LinkedIn exclusion removed
    - If no patches needed: SUMMARY explicitly documents "no-change verdict"
    - Worker dispatch for `followup_dm` inside LinkedIn branch verified (grep from 13-01)
    - pnpm typecheck clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — LinkedIn followup_dm end-to-end + Reddit regression</name>
  <read_first>
    - src/lib/action-worker/worker.ts (full dispatch region)
    - src/lib/action-worker/actions/linkedin-dm-executor.ts (the function mocked in the test)
    - src/app/api/cron/check-replies/route.ts (cron path confirmed in Task 1)
    - Any existing worker tests (`grep -rn "describe.*worker" src/lib/action-worker/__tests__/`) — mirror their shape
    - Any existing cron tests (`grep -rn "describe.*check-replies\\|followup" src/app/api/cron/`)
  </read_first>
  <behavior>
    - worker-linkedin-followup.test.ts: given an `actions` row with `action_type='followup_dm'`, `account.platform='linkedin'`, and a mocked `sendLinkedInDM` returning `{success:true}`, the worker invokes `sendLinkedInDM` (NOT `executeCUAction`) and marks action.status='completed'
    - worker-linkedin-followup.test.ts: given the same setup but `sendLinkedInDM` returns `{success:false, failureMode:'not_connected'}`, worker marks action.status='failed' and writes `failure_mode='not_connected'` into job_logs.metadata
    - worker-linkedin-followup.test.ts (Reddit regression): given `action_type='followup_dm'`, `account.platform='reddit'`, worker does NOT call `sendLinkedInDM` and routes through Haiku CU path (`executeCUAction` mock invoked)
    - check-replies/route.test.ts: given a LinkedIn prospect with `pipeline_status='connected'` and the prior `dm` aged 3 days, cron INSERTS a row with `action_type='followup_dm'` and `account_id` of the LinkedIn account (not filtered out)
    - check-replies/route.test.ts (Reddit regression): given a Reddit prospect with `pipeline_status='contacted'` and DM aged 3 days, cron still creates a `followup_dm` row (no regression)
  </behavior>
  <action>
    **A. Create `src/lib/action-worker/__tests__/worker-linkedin-followup.test.ts`:**

    Mock strategy:
    - Mock `./actions/linkedin-dm-executor` → `sendLinkedInDM` as `vi.fn()`
    - Mock `./computer-use` or `./actions/execute-cu-action` → `executeCUAction` as `vi.fn()` (locate exact path via `grep -n "executeCUAction" src/lib/action-worker/worker.ts`)
    - Mock `@/lib/supabase/server` or the service-role client factory the worker uses (grep for `createClient` in worker.ts)
    - Mock `@/lib/gologin` page/browser acquisition to return a stub Page

    Construct an `action` fixture + `account` fixture + `prospect` fixture, call the worker's entry function with the action id, assert on which executor was invoked. Use the same pattern as existing worker tests if any are found in the grep.

    If the worker's entry function is not easily isolable (e.g. it's a cron handler itself), test at the dispatch-function level — refactor worker.ts to export a narrow `dispatchActionPlatform(action, account, prospect, page)` function that Task 2 can call directly. Minimise refactor: one exported helper wrapping lines 258-336 of current worker.ts. Cite `// Exported for LNKD-04 integration test — see 13-04-PLAN.md Task 2`.

    **B. Create `src/app/api/cron/check-replies/__tests__/route.test.ts` (or extend existing):**

    Mock supabase client; seed two in-memory prospect rows (one LinkedIn `pipeline_status='connected'`, one Reddit `pipeline_status='contacted'`), both with a 3-day-old prior `dm` action. Call `GET` with valid Bearer. Assert the supabase `insert` mock was called with `action_type='followup_dm'` for BOTH prospects.

    Include a third prospect with `account.platform='linkedin'` but `pipeline_status='new'` → assert NOT eligible (no followup_dm created).

    Include a fourth prospect with `account.platform='linkedin'` and `prospects.pipeline_status='unreachable'` (added by 13-05) → assert NOT eligible (LNKD-06 intent extends to followups — unreachable prospects should not receive followups either). If the cron currently doesn't filter on `pipeline_status='unreachable'`, ADD that filter: `.neq('pipeline_status','unreachable')`. Cite `// LNKD-04 + LNKD-06 joint: unreachable prospects get no followups either`.
  </action>
  <verify>
    <automated>pnpm test -- worker-linkedin-followup check-replies &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    - `worker-linkedin-followup.test.ts` exists with ≥3 assertions (LinkedIn success, LinkedIn failure, Reddit regression); all green
    - `check-replies/__tests__/route.test.ts` exists (or extended) with ≥4 assertions (LinkedIn eligible, Reddit eligible, LinkedIn pipeline_status='new' excluded, LinkedIn pipeline_status='unreachable' excluded); all green
    - If worker refactor occurred: `grep -q "export function dispatchActionPlatform\\|export async function dispatchAction" src/lib/action-worker/worker.ts` succeeds; old worker behavior unchanged for all other callers
    - If cron extended to exclude unreachable: `grep -q "unreachable" src/app/api/cron/check-replies/route.ts`
    - Reddit followup_dm regression proven by test
    - pnpm typecheck clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron caller → /api/cron/check-replies | Bearer CRON_SECRET auth (existing) |
| Cron → actions table | Service-role client, bypasses RLS (existing pattern) |
| Worker dispatch → LinkedIn DM executor | In-process call; no new trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Spoofing | /api/cron/check-replies | accept | Existing Bearer CRON_SECRET check is unchanged; no new auth surface introduced by this plan. |
| T-13-04-02 | Tampering | Removing a platform filter could inadvertently broaden followup eligibility beyond intended rules | mitigate | Test case ASSERTS only `pipeline_status='connected'` (LinkedIn) and `pipeline_status='contacted'` (Reddit) are eligible. `pipeline_status='new'` and `pipeline_status='unreachable'` explicitly excluded by test. Filter broadening is test-gated. |
| T-13-04-03 | Information Disclosure | Reusing `action.content` across Reddit→LinkedIn could leak Reddit-voiced copy into a LinkedIn DM | accept | Cron generates fresh content per followup using the same generator as first-DM (platform-aware via account row). No content re-use across platforms because each prospect is tied to one account, which is single-platform. |
| T-13-04-04 | Repudiation | Follow-up DM failure silent on LinkedIn | mitigate | 13-01 already writes `failure_mode` into job_logs; this plan's test asserts that pathway. |
| T-13-04-05 | Elevation of Privilege | Cron refactor could expose service-role client path | accept | No refactor of auth or client construction; only filter-column edits. Service-role surface unchanged. |
| T-13-04-06 | Denial of Service | LinkedIn followup flood if `daily_dm_limit` not enforced for followup_dm | mitigate | Migration 00017 RPC routes `followup_dm` → `dm_count`/`daily_dm_limit` for LinkedIn (verified in 13-01 Task 1.D). Cap enforced. |
| T-13-04-07 | Replay | Duplicate followup_dm rows for the same prospect on the same day | mitigate | Existing cron should have a uniqueness guard; if not, Task 1 audit reveals it. Existing Phase 4 behavior should cover both platforms uniformly once platform filter removed. |
</threat_model>

<verification>
1. `pnpm typecheck && pnpm test` — all green
2. Audit comment / SUMMARY documents exact cron file path and verdict (no-change vs patched)
3. Manual E2E: backdate a LinkedIn DM to 3 days ago (via SQL on dev branch), manually POST to `/api/cron/check-replies` with Bearer → confirm `actions` row inserted with `action_type='followup_dm'`, `account.platform='linkedin'`
4. Manual E2E: trigger worker for that action → observe `sendLinkedInDM` invocation in logs (not Haiku CU), action.status='completed' or typed failure_mode
5. Manual regression: backdate a Reddit DM → cron creates `followup_dm` row → worker executes via Haiku CU (unchanged)
</verification>

<success_criteria>
- LNKD-04 complete: LinkedIn prospects receive day 3/7/14 follow-ups via the new LinkedIn DM executor from 13-01
- Reddit follow-ups regression-tested (unchanged behavior)
- `pipeline_status='unreachable'` prospects excluded from followup creation (LNKD-06 reinforcement)
- No new worker logic — this plan is wiring + tests; actual DM execution is 13-01's path
- pnpm typecheck + full test suite green
</success_criteria>

<output>
Create `.planning/phases/13-linkedin-action-expansion/13-04-SUMMARY.md` documenting: exact cron file path audited, verdict (no-change or patch summary with line refs), whether a worker dispatch export was introduced for testability, and the integration-test coverage matrix (LinkedIn-success, LinkedIn-fail, Reddit-regression, pipeline_status filters).
</output>
</content>
</invoke>