---
phase: 16-mechanism-cost-engine-schema
plan: 04
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/features/monitoring/lib/classification-pipeline.ts
  - src/features/onboarding/actions/save-onboarding.ts
autonomous: true
requirements: [PRIC-02]
tags: [refactor, monitoring, onboarding, mechanism_id]

must_haves:
  truths:
    - "classification-pipeline.ts no longer references `signal_type`; uses `mechanism_id` filters"
    - "save-onboarding.ts writes `mechanism_id` + `frequency` (not `signal_type` / `credits_per_day`)"
    - "pnpm typecheck passes after both edits"
    - "Onboarding inserts hit a mechanism_id that exists in mechanism_costs (FK satisfied)"
  artifacts:
    - path: "src/features/monitoring/lib/classification-pipeline.ts"
      provides: "Refactored monitoring filter using mechanism_id"
      contains: "mechanism_id"
    - path: "src/features/onboarding/actions/save-onboarding.ts"
      provides: "Refactored onboarding insert using mechanism_id + frequency"
      contains: "mechanism_id"
  key_links:
    - from: "save-onboarding.ts"
      to: "monitoring_signals.mechanism_id FK -> mechanism_costs"
      via: "INSERT with mechanism_id in {R1, R3}"
      pattern: 'mechanism_id:\s*"R[13]"'
    - from: "classification-pipeline.ts"
      to: "monitoring_signals shape (post-PLAN-01 migration)"
      via: ".select(\"mechanism_id, value\")"
      pattern: '\.select\("mechanism_id'
---

<objective>
Refactor the two surviving consumers of the legacy `signal_type` column flagged by RESEARCH.md §3 as REFACTOR (not in CONTEXT.md D-19 deletion list because the surrounding code paths — `webhooks/apify/route.ts` and the onboarding flow — survive).

Purpose: PRIC-02 — every read/write against `monitoring_signals` migrates from the legacy `signal_type` enum to the new `mechanism_id` text FK + `frequency` interval, satisfying the FK to `mechanism_costs` seeded in PLAN 01.
Output: classification-pipeline.ts and save-onboarding.ts compile cleanly against the new schema; downstream pipelines continue to function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/16-mechanism-cost-engine-schema/16-CONTEXT.md
@.planning/phases/16-mechanism-cost-engine-schema/16-RESEARCH.md
@src/features/monitoring/lib/classification-pipeline.ts
@src/features/onboarding/actions/save-onboarding.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor classification-pipeline.ts to use mechanism_id</name>
  <files>src/features/monitoring/lib/classification-pipeline.ts</files>
  <read_first>
    - src/features/monitoring/lib/classification-pipeline.ts (full file; the legacy filters are around lines 206/211/214 — verify exact line numbers on read)
    - 16-RESEARCH.md §3 (consumer enumeration; this file is in the REFACTOR list, not DELETE)
    - 16-CONTEXT.md D-19 (clean-slate deletion mandate; this file is preserved because webhooks/apify/route.ts depends on it)
  </read_first>
  <behavior>
    - The `.select("signal_type, value")` call is replaced with `.select("mechanism_id, value")`
    - Filters that previously matched `signal_type === "reddit_keyword"` and `signal_type === "competitor"` now match `mechanism_id === "R3"` (Competitor mention) — both legacy values map to R3 per RESEARCH.md §3
    - The semantic mapping is documented inline as a comment so future readers do not lose the intent
    - No other behavior changes
  </behavior>
  <action>
    Open `src/features/monitoring/lib/classification-pipeline.ts` and read it end-to-end before editing. Confirm the three approximate line numbers (206/211/214) by inspecting the actual content; the structure is:

    1. A `.select("signal_type, value")` call — replace with `.select("mechanism_id, value")`.
    2. A `.filter((s) => s.signal_type === "reddit_keyword")` call — replace with `.filter((s) => s.mechanism_id === "R3")`.
    3. A `.filter((s) => s.signal_type === "competitor")` call — replace with `.filter((s) => s.mechanism_id === "R3")`.

    Mapping rationale (per RESEARCH.md §3):
    - Legacy `competitor` -> R3 "Competitor mention" (direct semantic match)
    - Legacy `reddit_keyword` -> R3 "Competitor mention" (closest semantic match for the legacy generic keyword search; the original `reddit_keyword` was a catch-all that has no 1:1 successor, and R3 is the closest survivor)

    **Decision point for the executor:** if the two filter results are merged downstream (e.g. concatenated into a single signal stream), collapsing both onto R3 is correct and the filters can even be merged into one. If the two results feed distinct downstream branches that must remain semantically distinct, prefer mapping `reddit_keyword -> R4` (Question pattern (custom)) instead. Read the surrounding 50 lines of consumer code before deciding; document the chosen mapping in a `// MAPPING:` inline comment immediately above each replaced filter.

    Do NOT touch any other line in the file. Do NOT change typings/imports unless the new field name forces a TypeScript update — if it does, narrow the change to the smallest possible diff.

    After editing, run:
    ```bash
    pnpm typecheck
    ```
    Any TS error here likely means the row type for `monitoring_signals` is generated from Supabase types and must be regenerated, OR a local interface needs its `signal_type` field renamed. Adjust the local interface only — do not regenerate Supabase types in this plan.
  </action>
  <verify>
    <automated>! grep -q "signal_type" src/features/monitoring/lib/classification-pipeline.ts && grep -q "mechanism_id" src/features/monitoring/lib/classification-pipeline.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "signal_type" src/features/monitoring/lib/classification-pipeline.ts` returns nonzero (no match)
    - `grep -q "mechanism_id" src/features/monitoring/lib/classification-pipeline.ts` returns success
    - `grep -E "mechanism_id === \"R(3|4)\"" src/features/monitoring/lib/classification-pipeline.ts` matches at least twice
    - `grep -q "// MAPPING:" src/features/monitoring/lib/classification-pipeline.ts` returns success (decision documented)
    - `pnpm typecheck` exits 0
    - The git diff for this file is bounded to the three target replacements (and the mapping comments) — no incidental refactor
  </acceptance_criteria>
  <done>classification-pipeline.ts compiles against the new `monitoring_signals` shape with documented mappings.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor save-onboarding.ts to write mechanism_id + frequency</name>
  <files>src/features/onboarding/actions/save-onboarding.ts</files>
  <read_first>
    - src/features/onboarding/actions/save-onboarding.ts (full file; the inserts are around lines 71 and 77 — verify exact lines on read)
    - 16-RESEARCH.md §3 (mapping rationale: R1 = Subreddit firehose, R3 = Competitor mention)
    - 16-CONTEXT.md (FK semantics on monitoring_signals.mechanism_id)
    - PLAN 01 migration body (default `frequency` is `'6 hours'`, so passing `"6 hours"` matches the default exactly and the column is NOT NULL)
  </read_first>
  <behavior>
    - The two `monitoring_signals` insert payloads now write `mechanism_id` (not `signal_type`)
    - Both inserts also write `frequency: "6 hours" as const` (matches the migration default)
    - Any `credits_per_day` field is dropped (column was dropped in PLAN 01 migration)
    - No `config` field is added (column has DB default `'{}'`)
    - Mapping (per RESEARCH.md §3):
      - Legacy `signal_type: "subreddit"` -> `mechanism_id: "R1"` (Subreddit firehose)
      - Legacy `signal_type: "reddit_keyword"` -> `mechanism_id: "R3"` (Competitor mention; closest semantic match — same mapping decision as classification-pipeline.ts in Task 1)
  </behavior>
  <action>
    Open `src/features/onboarding/actions/save-onboarding.ts` and read it end-to-end. Locate the two insert payload literals (approximately lines 71 and 77 — confirm exact lines on read). They look approximately like:

    ```ts
    {
      // ...
      signal_type: "reddit_keyword" as const,
      // ...
    }
    ```

    and

    ```ts
    {
      // ...
      signal_type: "subreddit" as const,
      // ...
    }
    ```

    Replacements:

    1. The `signal_type: "reddit_keyword" as const` line becomes:
       ```ts
       mechanism_id: "R3" as const,
       frequency: "6 hours" as const,
       ```

    2. The `signal_type: "subreddit" as const` line becomes:
       ```ts
       mechanism_id: "R1" as const,
       frequency: "6 hours" as const,
       ```

    3. If either payload still has a `credits_per_day` field, REMOVE that line entirely (the column was dropped in PLAN 01 migration). Do not replace it; the burn engine (PLAN 03) computes the daily burn from `mechanism_costs` × `frequency` at read time.

    4. Do NOT add a `config` field; the DB default `'{}'` covers it (per PLAN 01 migration).

    Hard rules:
    - Do NOT change any other field in either payload (keep `user_id`, `value`, `active`, etc.).
    - Do NOT touch surrounding validation, Zod schemas, or RLS error handling unless a TypeScript error forces a narrow update.
    - If a local TypeScript interface or Zod schema explicitly types these inserts (e.g. `MonitoringSignalInsert`), update it to match the new shape — narrow the diff to the minimum.

    After editing, run:
    ```bash
    pnpm typecheck
    ```

    The test gate (FK-level): the dev DB now has `mechanism_costs` rows for `R1` and `R3` (PLAN 01). Inserting these two rows from onboarding will satisfy the FK. We do NOT execute a live insert here — that's a runtime concern. The acceptance criteria are static (grep + typecheck).
  </action>
  <verify>
    <automated>! grep -q "signal_type" src/features/onboarding/actions/save-onboarding.ts && grep -E 'mechanism_id:\s*"R(1|3)"' src/features/onboarding/actions/save-onboarding.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "signal_type" src/features/onboarding/actions/save-onboarding.ts` returns nonzero (no match)
    - `grep -q "credits_per_day" src/features/onboarding/actions/save-onboarding.ts` returns nonzero (no match — column was dropped)
    - `grep -cE 'mechanism_id:\s*"R(1|3)"' src/features/onboarding/actions/save-onboarding.ts` returns `2` (one R1 insert, one R3 insert)
    - `grep -cE 'frequency:\s*"6 hours"' src/features/onboarding/actions/save-onboarding.ts` returns `2`
    - `pnpm typecheck` exits 0
    - The git diff for this file is bounded to the two payload replacements
  </acceptance_criteria>
  <done>save-onboarding.ts writes the new monitoring_signals shape; FK to mechanism_costs is satisfied at runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhooks/apify -> classification-pipeline | Server-side scraped intent stream filtered by mechanism_id |
| onboarding action -> monitoring_signals | Authenticated user write through Supabase RLS, FK-checked against mechanism_costs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | monitoring_signals.mechanism_id (FK orphan inserts) | mitigate | After this plan, save-onboarding.ts writes only `R1` and `R3`, both seeded in PLAN 01. The `REFERENCES mechanism_costs(mechanism_id) ON DELETE RESTRICT NOT NULL` constraint rejects any orphan write. RLS still applies to the user_id column. |
| T-16-13 | Information Disclosure | classification-pipeline filter drift | accept | The R3 collapse merges two legacy categories. Downstream consumers receive a strict subset of the previous data; no new data is exposed across user boundaries (RLS unchanged). Documented inline via `// MAPPING:` comment. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -r "signal_type" src/features/monitoring/lib/classification-pipeline.ts src/features/onboarding/actions/save-onboarding.ts` returns no matches.
2. `grep -r "credits_per_day" src/features/onboarding/actions/save-onboarding.ts` returns no matches.
3. `pnpm typecheck` is green across the repo.
4. PLAN 05 (final invariant gate) will catch any stray reference to `signal_type` elsewhere in `src/`.
5. Phase commits this plan as `refactor(16): migrate classification-pipeline + onboarding to mechanism_id` (D-25 commit 4).
</verification>

<success_criteria>
- classification-pipeline.ts uses `mechanism_id` filters with documented mapping
- save-onboarding.ts writes `mechanism_id` + `frequency` and drops `credits_per_day`
- pnpm typecheck green
- PLAN 05 (deletion + final gate) can proceed
</success_criteria>

<output>
After completion, create `.planning/phases/16-mechanism-cost-engine-schema/16-04-SUMMARY.md` documenting: confirmed line numbers for the three replacements in classification-pipeline.ts, the two replacements in save-onboarding.ts, the chosen mapping decision (`reddit_keyword -> R3` vs `R4`), and any TypeScript interface updates required to satisfy the new shape.
</output>
