---
phase: 16-mechanism-cost-engine-schema
plan: 05
type: execute
wave: 3
depends_on: [16-02, 16-03, 16-04]
files_modified:
  - src/app/api/cron/monitor-reddit/route.ts
  - src/app/api/cron/monitor-linkedin/route.ts
  - src/app/api/cron/monitor-linkedin/route.test.ts
  - src/app/(app)/signals/page.tsx
  - src/features/monitoring/actions/settings-actions.ts
  - src/features/monitoring/components/sources-panel.tsx
  - vercel.json
autonomous: true
requirements: [PRIC-02, PRIC-03]
tags: [deletion, cleanup, cron, vercel]

must_haves:
  truths:
    - "monitor-reddit, monitor-linkedin cron route directories deleted"
    - "signals page directory deleted"
    - "monitoring/actions and monitoring/components directories deleted; lib/ and __fixtures__/ survive"
    - "vercel.json contains exactly 10 cron entries (was 12); valid JSON"
    - "0 grep hits for `signal_type`, `MonitoringSignalType`, `MONITORING_COSTS` in src/"
    - "pnpm build, typecheck, lint all green"
  artifacts:
    - path: "vercel.json"
      provides: "Cron schedule with monitor-* entries removed"
      contains: '"path":'
  key_links:
    - from: "vercel.json"
      to: "remaining cron routes"
      via: "JSON cron[] array"
      pattern: '"crons"'
---

<objective>
Clean-slate deletion per CONTEXT.md D-19: remove legacy monitoring cron routes, the signals page, monitoring actions+components, and the matching `vercel.json` entries. Final invariant gate: zero source references to deleted symbols and a green build.

Purpose: PRIC-02 / PRIC-03 — close out Phase 16 with no dead code paths or orphan cron schedules referencing the legacy monitoring stack.
Output: Filesystem deletions + a 10-cron `vercel.json` (was 12) + a passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/16-mechanism-cost-engine-schema/16-CONTEXT.md
@.planning/phases/16-mechanism-cost-engine-schema/16-RESEARCH.md
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete monitor-reddit and monitor-linkedin cron route files</name>
  <files>src/app/api/cron/monitor-reddit/route.ts, src/app/api/cron/monitor-linkedin/route.ts, src/app/api/cron/monitor-linkedin/route.test.ts</files>
  <read_first>
    - 16-CONTEXT.md D-19 (clean-slate deletion mandate)
    - 16-RESEARCH.md §3 (consumer enumeration — confirms these routes are in the DELETE list)
  </read_first>
  <action>
    Delete the legacy monitoring cron routes and any co-located tests:

    ```bash
    rm src/app/api/cron/monitor-reddit/route.ts
    rm src/app/api/cron/monitor-linkedin/route.ts
    rm src/app/api/cron/monitor-linkedin/route.test.ts
    rmdir src/app/api/cron/monitor-reddit 2>/dev/null || true
    rmdir src/app/api/cron/monitor-linkedin 2>/dev/null || true
    ```

    The `rmdir` calls succeed only if the directories are empty after the file removals. If any unexpected file remains, list it and STOP — do not force-remove without inspection.
  </action>
  <verify>
    <automated>test ! -d src/app/api/cron/monitor-reddit && test ! -d src/app/api/cron/monitor-linkedin</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/app/api/cron/monitor-reddit/route.ts` succeeds
    - `test ! -f src/app/api/cron/monitor-linkedin/route.ts` succeeds
    - `test ! -f src/app/api/cron/monitor-linkedin/route.test.ts` succeeds
    - `test ! -d src/app/api/cron/monitor-reddit` succeeds
    - `test ! -d src/app/api/cron/monitor-linkedin` succeeds
    - Sibling cron routes (e.g. `src/app/api/cron/credit-burn`) are untouched
  </acceptance_criteria>
  <done>Both monitor-* cron route directories are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the legacy signals page</name>
  <files>src/app/(app)/signals/page.tsx</files>
  <read_first>
    - 16-CONTEXT.md D-19
    - 16-RESEARCH.md §3
  </read_first>
  <action>
    Delete the signals page and its directory if empty:

    ```bash
    rm "src/app/(app)/signals/page.tsx"
    rmdir "src/app/(app)/signals" 2>/dev/null || true
    ```

    The path contains route-group parens; quote the path in bash. If the directory contains additional files (loading.tsx, layout.tsx, etc.) that were not declared in the work brief, list them and STOP for human review — do NOT auto-delete unlisted files.
  </action>
  <verify>
    <automated>test ! -d "src/app/(app)/signals"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f "src/app/(app)/signals/page.tsx"` succeeds
    - `test ! -d "src/app/(app)/signals"` succeeds
    - Sibling route-group pages (e.g. `src/app/(app)/page.tsx`, dashboards) are untouched
  </acceptance_criteria>
  <done>Signals route is gone from the app router.</done>
</task>

<task type="auto">
  <name>Task 3: Delete monitoring actions + components, preserve lib/ and __fixtures__/</name>
  <files>src/features/monitoring/actions/settings-actions.ts, src/features/monitoring/components/sources-panel.tsx</files>
  <read_first>
    - 16-CONTEXT.md D-19
    - 16-RESEARCH.md §3 (confirms `lib/classification-pipeline.ts` and `__fixtures__/` survive)
  </read_first>
  <action>
    Delete the legacy server actions and the sources panel UI; keep the lib/ and __fixtures__/ subtrees alive (PLAN 04 refactored classification-pipeline.ts; fixtures are still referenced by tests).

    ```bash
    rm src/features/monitoring/actions/settings-actions.ts
    rm src/features/monitoring/components/sources-panel.tsx
    rmdir src/features/monitoring/actions 2>/dev/null || true
    rmdir src/features/monitoring/components 2>/dev/null || true
    ```

    Pre-flight: list the contents of `actions/` and `components/` first. If there are additional files beyond the two named, STOP — they were not in the deletion contract for this phase.
  </action>
  <verify>
    <automated>test ! -d src/features/monitoring/actions && test ! -d src/features/monitoring/components && test -d src/features/monitoring/lib && test -d src/features/monitoring/__fixtures__</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/features/monitoring/actions/settings-actions.ts` succeeds
    - `test ! -f src/features/monitoring/components/sources-panel.tsx` succeeds
    - `test ! -d src/features/monitoring/actions` succeeds
    - `test ! -d src/features/monitoring/components` succeeds
    - `test -d src/features/monitoring/lib` succeeds (preserved)
    - `test -d src/features/monitoring/__fixtures__` succeeds (preserved)
    - `test -f src/features/monitoring/lib/classification-pipeline.ts` succeeds (refactored by PLAN 04, still present)
  </acceptance_criteria>
  <done>Legacy monitoring actions/components are gone; lib + fixtures survive.</done>
</task>

<task type="auto">
  <name>Task 4: Edit vercel.json to remove monitor-* cron entries</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json (current cron[] array; ~12 entries; monitor-reddit ~line 18, monitor-linkedin ~line 46)
    - 16-CONTEXT.md D-19
  </read_first>
  <action>
    Use the Edit tool (NOT Write — preserves the surrounding file) to remove exactly two cron object entries from `vercel.json`:

    1. The entry whose `path` is `"/api/cron/monitor-reddit"` (around line 18).
    2. The entry whose `path` is `"/api/cron/monitor-linkedin"` (around line 46).

    Each entry typically looks like:
    ```json
    {
      "path": "/api/cron/monitor-reddit",
      "schedule": "..."
    },
    ```

    Remove the entire object literal AND the trailing comma if it leaves a dangling `,` before `]`. The remaining 10 cron entries must form valid JSON (no trailing comma after the last entry, no double commas where one was removed mid-array).

    After editing, validate the JSON:
    ```bash
    python -c "import json; json.load(open('vercel.json'))"
    ```

    If parsing fails, restore the file from git and re-edit more carefully.
  </action>
  <verify>
    <automated>! grep -E "monitor-reddit|monitor-linkedin" vercel.json && python -c "import json,sys; sys.exit(0 if len(json.load(open('vercel.json')).get('crons',[])) == 10 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "monitor-reddit|monitor-linkedin" vercel.json` returns nonzero (no match)
    - `python -c "import json; json.load(open('vercel.json'))"` exits 0 (valid JSON)
    - `python -c "import json; print(len(json.load(open('vercel.json'))['crons']))"` prints `10` (was 12, minus 2)
    - Other cron entries (credit-burn, etc.) remain present and unchanged
  </acceptance_criteria>
  <done>vercel.json has exactly 10 cron entries and is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 5: Final invariant gate — grep + typecheck + lint + build</name>
  <files></files>
  <read_first>
    - 16-VALIDATION.md (per-task verification map; final gate criteria)
    - 16-CONTEXT.md D-19
  </read_first>
  <action>
    Run the final invariant grep gate. Any single hit is a hard fail and the executor must fix the offending file before proceeding:

    ```bash
    grep -rE "signal_type|MonitoringSignalType|MONITORING_COSTS" src/ && echo "FAIL: legacy reference still present" && exit 1
    ```

    Then the build/lint/type gates:

    ```bash
    pnpm typecheck
    pnpm lint
    pnpm build
    ```

    All three must exit 0. If any fail, the executor diagnoses the regression caused by the deletions in Tasks 1-4 (most likely: a stray import of a deleted file). Fix it in place; do NOT revert the deletions.

    Cron sanity (manual sweep, not gating): confirm `vercel.json` still references all surviving cron routes by name (e.g. credit-burn) and none of the surviving routes import from a deleted file.
  </action>
  <verify>
    <automated>! grep -rE "signal_type|MonitoringSignalType|MONITORING_COSTS" src/ && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "signal_type|MonitoringSignalType|MONITORING_COSTS" src/` returns nonzero (no matches anywhere in src/)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `pnpm build` exits 0
    - No new files were created during this task (it's a verification gate)
  </acceptance_criteria>
  <done>Phase 16 invariants hold across the codebase; build is green; no legacy symbols remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> Next.js build | App router resolves only surviving route files |
| vercel.json -> cron scheduler | Vercel reads cron[] at deploy time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | Wrong-branch deletion | accept | This plan is filesystem-only and touches NO database. CLAUDE.md `development` branch rule still applies for the eventual git push, but the deletions themselves carry no DB risk. |
| T-16-11 | Denial of Service | Build break from orphan import | mitigate | Task 5 final gate runs `pnpm typecheck && pnpm lint && pnpm build`; any orphan import from a deleted file fails the gate and must be fixed before phase commit. |
| T-16-12 | Tampering | vercel.json JSON corruption | mitigate | Task 4 verification runs `python -m json` to ensure valid JSON post-edit and asserts `len(crons) == 10`. |
</threat_model>

<verification>
After all 5 tasks complete:

1. The two monitor-* cron routes are gone (Task 1).
2. The legacy signals page is gone (Task 2).
3. monitoring/actions and monitoring/components are gone; lib/ and __fixtures__/ survive (Task 3).
4. vercel.json has exactly 10 cron entries and is valid JSON (Task 4).
5. Final grep gate is empty; typecheck + lint + build are all green (Task 5).
6. Phase commits this plan as `chore(16): clean-slate deletion of legacy monitoring stack` (D-25 commit 5).
</verification>

<success_criteria>
- Zero references to `signal_type`, `MonitoringSignalType`, `MONITORING_COSTS` in `src/`
- Zero monitor-* paths in `vercel.json`
- `pnpm build` green
- Phase 16 ready for cross-AI review and merge
</success_criteria>

<output>
After completion, create `.planning/phases/16-mechanism-cost-engine-schema/16-05-SUMMARY.md` documenting: list of files/directories deleted, final cron count in vercel.json (must be 10), and the exit codes of the typecheck/lint/build commands.
</output>
