---
phase: 16
slug: mechanism-cost-engine-schema
status: approved
shadcn_initialized: true
preset: radix-nova
created: 2026-04-27
reviewed_at: 2026-04-27T00:00:00Z
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## UI Surface Assessment

**This phase has no new user-facing UI surface. It is a deletion-only phase at the UI layer.**

Phase 16 is a DB schema migration (`mechanism_costs` table + 60-row seed) plus a backend refactor of `credit-burn.ts` around `mechanism_id`. The CONTEXT.md `<code_context>` block explicitly states: *"`live_stats` / dashboard — currently shows monitoring counts, not burn breakdown. **No UI cascade from this phase.**"*

Per CONTEXT.md `<deferred>`, the new `/signals` UI redesign + 27-mechanism configurator is owned by **Phase 22**. Phase 16 only **clears the slate** by deleting the legacy signal-management surface. No replacement pixels ship in this phase.

Downstream agents (planner, executor, checker) MUST treat this as a zero-UI-delta phase. The contract sections below are pre-populated from the existing design system for completeness and checker compliance.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json (detected) |
| Preset | radix-nova | components.json |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | Phosphor (with Lucide present in legacy components) | components.json `"iconLibrary": "phosphor"` |
| Font | System sans (var(--font-sans)) | globals.css |

Phase 16 introduces no new icon, font, or component library usage.

---

## Spacing Scale

Inherited from existing system, no changes in Phase 16:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none

---

## Typography

Inherited. No new typographic elements introduced in Phase 16.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 14px | 500 | 1.4 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 700 | 1.2 |

Source: radix-nova preset defaults + existing usage patterns.

---

## Color

Inherited from existing globals.css CSS variables (oklch, radix-nova / stone base). No new color usage in Phase 16.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | oklch(1 0 0) / dark surface | Background, main content area |
| Secondary (30%) | oklch(0.985 0.001 106.423) | Sidebar, card surfaces |
| Accent (10%) | oklch(0.457 0.24 277.023) — violet/purple primary | Primary buttons, active nav items, focus rings |
| Destructive | oklch(0.577 0.245 27.325) — red | Destructive confirmations only |

Accent reserved for: primary action buttons, active sidebar navigation, focus ring on interactive elements.

---

## Copywriting Contract

Phase 16 introduces no new copy. The deletions remove existing UI strings entirely; they are not replaced in this phase.

| Element | Copy | Status |
|---------|------|--------|
| Primary CTA | n/a — no new CTA in Phase 16 | Not applicable |
| Empty state heading | n/a — no new empty state | Not applicable |
| Empty state body | n/a | Not applicable |
| Error state | n/a — no new error surfaces | Not applicable |
| Destructive confirmation | n/a — no new destructive actions | Not applicable |

**Sidebar nav note:** If `/signals` is currently linked from `AppSidebar`, plan-phase / executor MUST remove the dead nav entry as part of the deletion (orphan link cleanup per CLAUDE.md §3 "Surgical Changes"). This is a copy/link removal, not new copy.

---

## Component Inventory

### Removed (deletion-only — no replacement in this phase)

| File | Reason | Visual Impact |
|------|--------|---------------|
| `src/app/(app)/signals/page.tsx` | Reads dropped `signal_type` column; rebuilt in Phase 22 | `/signals` route returns 404 between Phase 16 and Phase 22 (acceptable per `project_users_are_test_data`) |
| `src/features/monitoring/actions/settings-actions.ts` | Reads/writes `signal_type` | No standalone surface — actions consumed only by deleted page |
| `src/features/monitoring/components/*` (files referencing the deleted page) | Orphaned by page deletion | Plan-phase enumerates exact files via grep; any survivors stay |
| `src/app/api/cron/monitor-reddit/route.ts` (+ test) | Backend cron, no UI; listed for completeness | None (no UI) |
| `src/app/api/cron/monitor-linkedin/route.ts` (+ test) | Backend cron, no UI; listed for completeness | None (no UI) |
| `vercel.json` cron entries for `monitor-reddit` + `monitor-linkedin` | Config, no UI | None |

### Modified (no visual delta)

None in the UI layer. All modifications are backend (`credit-burn.ts`, `credit-burn/route.ts`, `types.ts`).

### New (non-UI)

| File | Type | Notes |
|------|------|-------|
| `src/features/billing/lib/mechanism-costs.ts` | Library helper (cached cost lookup) | No component, no UI |
| `supabase/migrations/0002X_mechanism_costs.sql` | DB migration | No UI |

### No UI registry additions

No shadcn components are added or removed in Phase 16. No blocks fetched.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | None — no new components installed | not required |

No third-party registries declared. Registry vetting gate not applicable.

---

## Interaction Contracts

No new interactions are introduced. The legacy `/signals` page interactions (toggling signals, editing cadence) are removed wholesale; Phase 22 will define their replacements. No new modals, drawers, toasts, or loading states are added in Phase 16.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — no new copy; deletions only (orphan nav link cleanup is a removal, not new copy)
- [ ] Dimension 2 Visuals: PASS — no new screens, no visual delta; `/signals` route 404 between Phase 16 and Phase 22 is intentional clean-slate
- [ ] Dimension 3 Color: PASS — no new color tokens; existing palette inherited
- [ ] Dimension 4 Typography: PASS — no new typographic elements
- [ ] Dimension 5 Spacing: PASS — no new layout elements
- [ ] Dimension 6 Registry Safety: PASS — no third-party registry usage

**Approval:** approved 2026-04-27

---

*Phase: 16-mechanism-cost-engine-schema*
*UI-SPEC generated: 2026-04-27*
*Verdict: Zero-UI-delta phase. DB schema + backend refactor + UI deletion only. `/signals` UI redesign deferred to Phase 22. All existing design tokens inherited.*
