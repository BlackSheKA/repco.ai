---
phase: 05-billing-onboarding-growth
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/features/prospects/components/kanban-board.tsx
  - src/features/prospects/components/kanban-column.tsx
  - src/features/prospects/components/prospect-card.tsx
  - src/features/prospects/components/prospect-detail.tsx
  - src/features/prospects/actions/update-prospect.ts
  - src/features/prospects/actions/export-csv.ts
  - src/app/(app)/prospects/page.tsx
  - src/app/(app)/prospects/[id]/page.tsx
autonomous: true
requirements: [PRSP-01, PRSP-02, PRSP-03, PRSP-04, PRSP-05, PRSP-06, DASH-04]

must_haves:
  truths:
    - "User can view prospects in kanban board with 6 stage columns"
    - "User can drag-and-drop prospect cards between columns"
    - "User can use Move to... dropdown for keyboard/mobile access"
    - "User can click a prospect card to see full detail page"
    - "User can add notes and tags to prospects on detail page"
    - "User can export all prospects as CSV"
    - "Dashboard shows prospect summary stats and estimated revenue"
  artifacts:
    - path: "src/features/prospects/components/kanban-board.tsx"
      provides: "Drag-and-drop kanban board with 6 columns"
      contains: "DragDropProvider"
    - path: "src/app/(app)/prospects/page.tsx"
      provides: "/prospects route with kanban board"
    - path: "src/app/(app)/prospects/[id]/page.tsx"
      provides: "/prospects/[id] detail route"
    - path: "src/features/prospects/actions/export-csv.ts"
      provides: "CSV export server action"
      contains: "use server"
  key_links:
    - from: "src/features/prospects/components/kanban-board.tsx"
      to: "src/features/prospects/actions/update-prospect.ts"
      via: "server action on drag-drop stage change"
      pattern: "updateProspectStage"
    - from: "src/features/prospects/actions/export-csv.ts"
      to: "papaparse"
      via: "CSV generation"
      pattern: "Papa.unparse"
---

<objective>
Build the prospect pipeline kanban board with drag-and-drop, prospect detail page, notes/tags, CSV export, and dashboard summary stats with revenue counter.

Purpose: Users manage their sales pipeline visually, track progress, and measure ROI.
Output: /prospects kanban page, /prospects/[id] detail page, CSV export, dashboard stats card.
</objective>

<execution_context>
@C:/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-billing-onboarding-growth/05-CONTEXT.md
@.planning/phases/05-billing-onboarding-growth/05-UI-SPEC.md

<interfaces>
From src/features/prospects/lib/types.ts (plan 01):
```typescript
export const PIPELINE_STAGES = ["detected", "engaged", "contacted", "replied", "converted", "rejected"] as const
export type PipelineStage = (typeof PIPELINE_STAGES)[number]
export interface ProspectWithSignal { id, user_id, platform, handle, display_name, bio, pipeline_status, notes, tags, ... }
```

From src/features/prospects/lib/pipeline.ts (plan 01):
```typescript
export function isValidStageTransition(from: PipelineStage, to: PipelineStage): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prospect server actions and CSV export</name>
  <files>
    src/features/prospects/actions/update-prospect.ts
    src/features/prospects/actions/export-csv.ts
  </files>
  <read_first>
    - src/features/prospects/lib/types.ts
    - src/features/prospects/lib/pipeline.ts
    - supabase/migrations/00002_initial_schema.sql
    - .planning/phases/05-billing-onboarding-growth/05-RESEARCH.md
  </read_first>
  <action>
1. Install: `pnpm add papaparse && pnpm add -D @types/papaparse`

2. Create `src/features/prospects/actions/update-prospect.ts` ("use server"):
- `updateProspectStage(prospectId: string, newStage: PipelineStage)`:
  - Get authenticated user
  - Validate stage transition via isValidStageTransition (fetch current stage first)
  - If invalid: return { error: "Invalid stage transition" }
  - Update prospects table: SET pipeline_status = newStage, updated_at = now()
  - Return { success: true, newStage }

- `updateProspectNotes(prospectId: string, notes: string)`:
  - Get authenticated user
  - Update prospects table: SET notes = notes, updated_at = now()
  - Return { success: true }

- `updateProspectTags(prospectId: string, tags: string[])`:
  - Get authenticated user
  - Update prospects table: SET tags = tags, updated_at = now()
  - Return { success: true }

3. Create `src/features/prospects/actions/export-csv.ts` ("use server"):
- `exportProspectsCSV()`:
  - Get authenticated user
  - Query all prospects for user with columns: handle, platform, pipeline_status, display_name, bio, notes, tags, created_at, updated_at
  - Order by created_at DESC
  - Use Papa.unparse with header: true and columns: ["handle", "platform", "pipeline_status", "display_name", "bio", "notes", "tags", "created_at"]
  - Convert tags array to comma-separated string before CSV generation
  - Return CSV string
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/prospects/actions/update-prospect.ts contains "use server"
    - src/features/prospects/actions/update-prospect.ts exports updateProspectStage, updateProspectNotes, updateProspectTags
    - src/features/prospects/actions/update-prospect.ts calls isValidStageTransition
    - src/features/prospects/actions/export-csv.ts contains "use server"
    - src/features/prospects/actions/export-csv.ts imports Papa from "papaparse" and calls Papa.unparse
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Server actions for stage transitions (validated), notes/tags updates, and CSV export with papaparse</done>
</task>

<task type="auto">
  <name>Task 2: Kanban board UI, prospect detail page, and dashboard stats</name>
  <files>
    src/features/prospects/components/kanban-board.tsx
    src/features/prospects/components/kanban-column.tsx
    src/features/prospects/components/prospect-card.tsx
    src/features/prospects/components/prospect-detail.tsx
    src/app/(app)/prospects/page.tsx
    src/app/(app)/prospects/[id]/page.tsx
    src/app/(app)/page.tsx
    src/app/(app)/settings/page.tsx
  </files>
  <read_first>
    - src/features/prospects/lib/types.ts
    - src/features/prospects/actions/update-prospect.ts
    - src/features/prospects/actions/export-csv.ts
    - src/features/dashboard/components/signal-card.tsx
    - src/app/(app)/page.tsx
    - src/app/(app)/settings/page.tsx
    - .planning/phases/05-billing-onboarding-growth/05-UI-SPEC.md
  </read_first>
  <action>
1. Install: `pnpm add @dnd-kit/react`
2. Add shadcn components: `pnpm dlx shadcn@latest add textarea scroll-area`

3. Create `src/features/prospects/components/prospect-card.tsx` ("use client"):
- Props: prospect (ProspectWithSignal), onMove (stage: PipelineStage) => void
- Card with compact layout: handle + platform badge, intent strength (FlameIndicator reuse), pipeline stage badge
- "Move to..." Select dropdown (shadcn Select) with all valid target stages
- onClick navigates to /prospects/[id]
- Drag handle touch target 48px height
- Mobile: hide drag handle below 768px, show "Move to..." dropdown always

4. Create `src/features/prospects/components/kanban-column.tsx` ("use client"):
- Props: stage (PipelineStage), prospects (ProspectWithSignal[]), onMove callback
- Column header: stage name (capitalized) + count badge
- Vertical scrollable list of ProspectCards
- Droppable zone using @dnd-kit/react Droppable
- Min-width 280px
- Empty column: muted placeholder text

5. Create `src/features/prospects/components/kanban-board.tsx` ("use client"):
- Props: initialProspects (ProspectWithSignal[])
- Uses @dnd-kit/react DragDropProvider
- State: prospects grouped by pipeline_status
- 6 KanbanColumns in horizontal scroll container
- On drag end: call updateProspectStage server action, optimistic UI update, revert on error with Sonner toast
- Horizontal scroll via ScrollArea on mobile/narrow viewports
- Gap 16px (md spacing) between columns

6. Create `src/features/prospects/components/prospect-detail.tsx` (server component shell + client interactive parts):
- Back button "Back to prospects" linking to /prospects
- Two-column layout above lg: left 2/3 for conversation history, right 1/3 for info
- Left column: list of actions (DMs, follow-ups) for this prospect with timestamps and content
- Right column:
  - Platform badge + handle
  - Bio text
  - Intent signal: post excerpt, intent strength, suggested angle
  - Pipeline status with "Move to..." dropdown
  - Notes: textarea, inline editable, auto-saves on blur via updateProspectNotes
  - Tags: comma-separated input, saves on Enter/blur via updateProspectTags
- Single column stack below lg

7. Create `src/app/(app)/prospects/page.tsx`:
- Server component with metadata title "Prospects"
- Query all prospects for user with intent_signal join
- Page header: "Prospects" heading left, "Export CSV" button right
- Export CSV: client-side handler that calls exportProspectsCSV() action, creates Blob from returned CSV string, triggers download via anchor click
- Empty state when no prospects: "No prospects yet" heading + "Prospects appear here when repco detects and engages with people looking for your product. Start by connecting your Reddit account." + "Connect account" CTA linking to /accounts
- Render KanbanBoard with fetched prospects

8. Create `src/app/(app)/prospects/[id]/page.tsx`:
- Server component, query single prospect with intent_signal join
- Query all actions for this prospect (DMs, follow-ups)
- Render ProspectDetail component
- 404 if prospect not found or wrong user

9. Update `src/app/(app)/page.tsx`:
- Add prospect summary stats card to dashboard grid
- Query: count by pipeline_status (detected, engaged, contacted, replied, converted, rejected)
- Query: avg_deal_value from users table
- Display card: "Prospects" heading, total count, replied count, converted count
- Revenue counter: "Est. revenue" label, value = converted count * avg_deal_value (Geist Mono font)
- If avg_deal_value not set: show "Set your average deal value in Settings to see estimated revenue." with link to /settings

10. Update `src/app/(app)/settings/page.tsx`:
- Add "Average Deal Value" section with number input
- Save to users.avg_deal_value via server action
- Label: "Used to calculate estimated revenue from conversions"
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/prospects/components/kanban-board.tsx contains "use client" and "DragDropProvider" from "@dnd-kit/react"
    - src/features/prospects/components/kanban-column.tsx renders column header with stage name and count
    - src/features/prospects/components/prospect-card.tsx has "Move to..." Select dropdown
    - src/app/(app)/prospects/page.tsx has "Export CSV" button and empty state with "No prospects yet"
    - src/app/(app)/prospects/[id]/page.tsx exists and queries single prospect
    - src/features/prospects/components/prospect-detail.tsx has notes textarea and tags input
    - src/app/(app)/page.tsx renders prospect stats card with "Est. revenue" label
    - src/app/(app)/settings/page.tsx has "Average Deal Value" input
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Kanban board with drag-and-drop and Move-to dropdown, prospect detail page with notes/tags/conversation, CSV export download, dashboard prospect stats with revenue counter, settings page deal value input</done>
</task>

</tasks>

<verification>
- pnpm typecheck passes
- /prospects route renders kanban board with 6 columns
- /prospects/[id] route renders detail page
- Dashboard shows prospect stats card
- CSV export downloads correctly
</verification>

<success_criteria>
- Kanban board shows 6 columns (detected through rejected) with drag-and-drop
- Prospect cards have Move to... dropdown for mobile/keyboard
- Prospect detail page shows conversation history, notes, tags
- CSV export generates valid file with all prospect data
- Dashboard stats card shows totals and estimated revenue
- Settings page has avg deal value input
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-onboarding-growth/05-05-SUMMARY.md`
</output>
