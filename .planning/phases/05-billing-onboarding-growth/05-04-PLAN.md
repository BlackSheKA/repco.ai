---
phase: 05-billing-onboarding-growth
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/api/cron/credit-burn/route.ts
  - src/lib/action-worker/worker.ts
  - src/features/billing/components/credit-balance.tsx
  - src/features/billing/components/credit-card.tsx
  - src/features/billing/components/upgrade-banner.tsx
  - src/features/billing/components/contextual-credit-prompt.tsx
  - src/components/shell/app-sidebar.tsx
  - src/app/(app)/page.tsx
  - src/features/actions/components/approval-queue.tsx
autonomous: true
requirements: [BILL-04, BILL-05, BILL-06, BILL-08, BILL-09]

must_haves:
  truths:
    - "Daily monitoring credit burn is deducted automatically via cron"
    - "Daily account credit burn for extra accounts (beyond 2 included) is deducted via cron"
    - "Action credits are deducted on action completion via deduct_credits RPC in action worker"
    - "Sidebar footer shows credit balance with burn rate, turns orange at <100, red at <50"
    - "Dashboard shows detailed credit card with balance, burn breakdown, and projected days"
    - "Warning banner appears on dashboard when credits < 50"
    - "Contextual prompt appears when approving a DM with low credit balance"
  artifacts:
    - path: "src/app/api/cron/credit-burn/route.ts"
      provides: "Daily cron for monitoring + account credit deduction"
      contains: "CRON_SECRET"
    - path: "src/lib/action-worker/worker.ts"
      provides: "Action worker with credit deduction on completion"
      contains: "deduct_credits"
    - path: "src/features/billing/components/credit-balance.tsx"
      provides: "Sidebar footer credit balance widget"
      min_lines: 20
    - path: "src/features/billing/components/credit-card.tsx"
      provides: "Dashboard credit summary card with burn breakdown"
      min_lines: 40
    - path: "src/features/billing/components/upgrade-banner.tsx"
      provides: "Warning banner at <50 credits"
    - path: "src/features/billing/components/contextual-credit-prompt.tsx"
      provides: "Inline prompt at action approval point"
  key_links:
    - from: "src/app/api/cron/credit-burn/route.ts"
      to: "supabase deduct_credits RPC"
      via: "rpc call for atomic deduction"
      pattern: "deduct_credits"
    - from: "src/lib/action-worker/worker.ts"
      to: "supabase deduct_credits RPC"
      via: "rpc call after successful action completion"
      pattern: "deduct_credits"
    - from: "src/components/shell/app-sidebar.tsx"
      to: "src/features/billing/components/credit-balance.tsx"
      via: "SidebarFooter renders CreditBalance"
      pattern: "CreditBalance"
---

<objective>
Implement the credit economy runtime: daily burn cron for monitoring and account credits, action-level deduction in the Phase 3 action worker, sidebar credit balance widget, dashboard credit card, and upgrade prompts.

Purpose: Credit awareness drives revenue -- users see spend constantly and get upgrade nudges at key moments.
Output: Credit burn cron, action worker credit deduction, sidebar balance widget, dashboard credit card, upgrade prompts.
</objective>

<execution_context>
@C:/Users/kamil/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kamil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-billing-onboarding-growth/05-CONTEXT.md
@.planning/phases/05-billing-onboarding-growth/05-UI-SPEC.md

<interfaces>
From src/features/billing/lib/types.ts (plan 01):
```typescript
export type ActionCreditType = "like" | "follow" | "public_reply" | "dm" | "followup_dm"
export const CREDIT_COSTS: Record<ActionCreditType, number> = {
  like: 0, follow: 0, public_reply: 15, dm: 30, followup_dm: 20,
}
export const MONITORING_COSTS: Record<string, number> = {
  reddit_keyword: 3, linkedin_keyword: 6, subreddit: 3, competitor: 3, profile_visitor: 3,
}
export const ACCOUNT_COSTS: Record<string, number> = { reddit: 3, linkedin: 5 }
export const INCLUDED_ACCOUNTS = 2
```

From src/features/billing/lib/credit-burn.ts (plan 01):
```typescript
export function calculateMonitoringBurn(signals: { signal_type: string; active: boolean }[]): number
export function calculateAccountBurn(accounts: { platform: string; active: boolean }[]): number
export function calculateDailyBurn(signals, accounts): number
```

From src/features/billing/lib/credit-costs.ts (plan 01):
```typescript
export function getActionCreditCost(actionType: ActionCreditType): number
```

From src/lib/action-worker/worker.ts (Phase 3):
```typescript
// Line ~211-212: After successful action completion
if (result.success) {
  await updateActionStatus(supabase, actionId, "completed", null)
  // Credit deduction should be inserted here
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credit burn cron and action worker credit deduction</name>
  <files>
    src/app/api/cron/credit-burn/route.ts
    src/lib/action-worker/worker.ts
  </files>
  <read_first>
    - src/app/api/cron/zombie-recovery/route.ts
    - src/lib/action-worker/worker.ts
    - src/features/billing/lib/credit-burn.ts
    - src/features/billing/lib/credit-costs.ts
    - src/features/billing/lib/types.ts
    - src/lib/logger.ts
    - supabase/migrations/00007_phase5_billing_onboarding.sql
  </read_first>
  <action>
1. Create `src/app/api/cron/credit-burn/route.ts`:
- Follow zombie-recovery cron pattern: Bearer token check against CRON_SECRET, correlationId, structured logging
- GET handler (Vercel Cron compatible)
- Service role Supabase client
- For each user with subscription_active = true OR trial_ends_at > now():
  a. Query monitoring_signals WHERE user_id = user.id AND active = true
  b. Query social_accounts WHERE user_id = user.id AND active = true
  c. Calculate monitoring burn via calculateMonitoringBurn(signals)
  d. Calculate account burn via calculateAccountBurn(accounts)
  e. Total = monitoring + account
  f. If total > 0: call supabase.rpc("deduct_credits", { p_user_id: user.id, p_amount: total, p_type: "monitoring_burn", p_description: `Daily burn: ${monitoringBurn} monitoring + ${accountBurn} accounts` })
  g. Log result per user (success with new balance, or insufficient credits)
- await logger.flush() before return
- Add to vercel.json cron config: schedule "0 0 * * *" (daily at midnight UTC)
- Return summary: { processed: N, total_deducted: N, insufficient: N }

2. Update `src/lib/action-worker/worker.ts` to deduct credits on action completion:
- Import `getActionCreditCost` from `@/features/billing/lib/credit-costs`
- Import `ActionCreditType` from `@/features/billing/lib/types`
- After the `updateActionStatus(supabase, actionId, "completed", null)` call on line ~212 (inside the `if (result.success)` block):
  a. Get credit cost: `const creditCost = getActionCreditCost(action.action_type as ActionCreditType)`
  b. If creditCost > 0: call `supabase.rpc("deduct_credits", { p_user_id: action.user_id, p_amount: creditCost, p_type: "action_spend", p_description: \`${action.action_type} action on ${action.platform}\` })`
  c. Log the deduction result (new balance or insufficient credits warning)
  d. Do NOT block action completion on credit failure -- the action already succeeded, just log a warning if credits insufficient
  </action>
  <verify>
    <automated>grep -c "deduct_credits" src/lib/action-worker/worker.ts src/app/api/cron/credit-burn/route.ts | grep -v ":0$" | wc -l | xargs test 2 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/cron/credit-burn/route.ts exists with GET handler
    - Contains Bearer token check against CRON_SECRET
    - Contains `supabase.rpc("deduct_credits"` call
    - Contains `calculateMonitoringBurn` and `calculateAccountBurn` imports
    - Contains `logger.flush()` before return
    - Returns JSON with processed, total_deducted, insufficient counts
    - src/lib/action-worker/worker.ts imports getActionCreditCost
    - src/lib/action-worker/worker.ts calls supabase.rpc("deduct_credits") after successful action completion
    - Credit deduction does not block action completion on failure
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Credit burn cron deducts monitoring + account credits daily for active users, action worker deducts action credits on completion via deduct_credits RPC</done>
</task>

<task type="auto">
  <name>Task 2: Credit balance widget, dashboard card, and upgrade prompts</name>
  <files>
    src/features/billing/components/credit-balance.tsx
    src/features/billing/components/credit-card.tsx
    src/features/billing/components/upgrade-banner.tsx
    src/features/billing/components/contextual-credit-prompt.tsx
    src/components/shell/app-sidebar.tsx
    src/app/(app)/page.tsx
    src/app/(app)/layout.tsx
    src/features/actions/components/approval-queue.tsx
  </files>
  <read_first>
    - src/components/shell/app-sidebar.tsx
    - src/app/(app)/page.tsx
    - src/app/(app)/layout.tsx
    - src/features/billing/lib/types.ts
    - src/features/billing/lib/credit-burn.ts
    - src/features/actions/components/approval-queue.tsx
    - .planning/phases/05-billing-onboarding-growth/05-UI-SPEC.md
  </read_first>
  <action>
1. Create `src/features/billing/components/credit-balance.tsx` ("use client"):
- Props: balance (number), dailyBurn (number)
- Link to /billing, styled with cn():
  - Default: text-muted-foreground
  - balance < 100: text-orange-500 (warning orange)
  - balance < 50: text-red-500 (destructive)
- Display: "{balance} credits . -{dailyBurn}/day"
- Use Geist Mono font for numbers (font-mono)
- Hover: underline + pointer cursor
- Block rounded-md px-2 py-1.5 text-sm

2. Create `src/features/billing/components/credit-card.tsx` ("use client"):
- Props: balance, monitoringBurn, accountBurn, actionBurn (avg daily), projectedDays
- shadcn Card with title "Credit Balance" (heading size 20px)
- Balance in large Geist Mono font (text-2xl font-mono)
- Burn breakdown:
  - "Monitoring: -{monitoringBurn}/day" 
  - "Accounts: -{accountBurn}/day"
  - "Actions: ~{actionBurn}/day (avg)"
  - "Total: -{total}/day"
- Projected days: "{projectedDays} days remaining" (colored: green >30, orange 7-30, red <7)
- If balance < 50: show "Buy credits" link to /billing

3. Create `src/features/billing/components/upgrade-banner.tsx` ("use client"):
- Props: balance (number)
- Only renders when balance < 50
- Yellow/orange warning banner at top of dashboard content area
- Copy: "Credits running low -- buy a pack or upgrade"
- Link to /billing with "Buy credits" CTA button
- Dismissible per session (useState, not localStorage)

4. Create `src/features/billing/components/contextual-credit-prompt.tsx` ("use client"):
- Props: actionCost (number), remainingCredits (number)
- Only renders when remainingCredits < 50 (or remainingCredits < actionCost * 2)
- Inline alert below DM content in approval queue
- Copy: "This DM costs {actionCost} credits. {remainingCredits} remaining."
- "Buy credits" link to /billing
- Muted background, text-sm

5. Update `src/components/shell/app-sidebar.tsx`:
- Add CreditBalance import
- Accept new props: creditBalance (number), dailyBurn (number)
- Render CreditBalance above user email in SidebarFooter
- Add "Billing" nav item: { label: "Billing", icon: CreditCard (from lucide-react), href: "/billing" }
- Update "Prospects" nav item href from "#" to "/prospects"

6. Update `src/app/(app)/layout.tsx`:
- Query user's credits_balance, monitoring_signals (active), and social_accounts (active)
- Calculate dailyBurn using calculateDailyBurn
- Pass creditBalance and dailyBurn to AppShell/AppSidebar

7. Update `src/app/(app)/page.tsx`:
- Query monitoring_signals and social_accounts for burn breakdown
- Calculate monitoring burn, account burn
- Query recent actions to estimate average daily action burn (count completed actions in last 7 days * avg cost / 7)
- Calculate projected days = Math.floor(credits_balance / totalDailyBurn) or Infinity if burn is 0
- Render UpgradeBanner with balance
- Render CreditCard with all burn data

8. Update `src/features/actions/components/approval-queue.tsx`:
- Import ContextualCreditPrompt
- Render ContextualCreditPrompt below DM content preview for each action card, passing the action's credit cost and user's remaining credits
- Add a comment in the approve handler noting: "Credits deducted on action completion in worker, not at approval time"
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/billing/components/credit-balance.tsx contains Link to "/billing"
    - src/features/billing/components/credit-balance.tsx contains conditional classes for balance < 100 (orange) and < 50 (red)
    - src/features/billing/components/credit-card.tsx displays balance, monitoringBurn, accountBurn, projectedDays
    - src/features/billing/components/upgrade-banner.tsx contains "Credits running low"
    - src/features/billing/components/contextual-credit-prompt.tsx contains "This DM costs"
    - src/components/shell/app-sidebar.tsx imports CreditBalance and renders it in SidebarFooter
    - src/components/shell/app-sidebar.tsx has "Billing" nav item with href "/billing"
    - src/components/shell/app-sidebar.tsx has "Prospects" nav item with href "/prospects" (not "#")
    - src/app/(app)/layout.tsx queries credits_balance and passes to AppShell
    - src/features/actions/components/approval-queue.tsx imports and renders ContextualCreditPrompt
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Credit balance in sidebar footer (color-coded thresholds), dashboard credit card with burn breakdown and projection, warning banner at <50 credits, contextual prompt at DM approval, approval queue integrated with credit prompt</done>
</task>

</tasks>

<verification>
- pnpm typecheck passes
- Sidebar footer shows credit balance widget
- Dashboard shows credit card with burn breakdown
- Credit burn cron endpoint returns valid JSON
- Action worker contains deduct_credits call after successful completion
</verification>

<success_criteria>
- Credit burn cron runs daily and deducts monitoring + account credits atomically
- Action worker deducts action credits on completion via deduct_credits RPC
- Sidebar credit balance always visible: turns orange at <100, red at <50
- Dashboard credit card shows balance, burn by layer, projected days remaining
- Warning banner at <50 credits on dashboard
- Contextual prompt at DM approval when credits low
- Billing and Prospects nav items active in sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-onboarding-growth/05-04-SUMMARY.md`
</output>
