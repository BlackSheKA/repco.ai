---
phase: 17.8
plan: 05
type: execute
wave: 3
depends_on: [17.8-01]
files_modified:
  - src/features/accounts/actions/account-actions.ts
  - src/features/accounts/components/connection-flow.tsx
  - src/features/accounts/components/account-list.tsx
  - src/features/accounts/actions/account-actions.test.ts
autonomous: true
requirements: [BPRX-14]
must_haves:
  truths:
    - "connectAccount(platform) — single-arg signature; no handle parameter"
    - "ConnectionFlow has no handle input field"
    - "On connect, social_accounts row is inserted with display_name='Reddit konto N' or 'LinkedIn konto N' (auto-numbered per user per platform)"
    - "No 'linkedin-${user.id.slice(0,8)}' placeholder remains anywhere"
    - "allocator receives a profileLabel (free-form), not a handle"
  artifacts:
    - path: "src/features/accounts/components/connection-flow.tsx"
      provides: "Two-CTA connect flow (no input)"
    - path: "src/features/accounts/actions/account-actions.ts"
      provides: "connectAccount(platform) single-arg signature + auto-numbered display_name"
  key_links:
    - from: "ConnectionFlow Connect Reddit button"
      to: "connectAccount('reddit')"
      via: "client onClick → server action"
      pattern: "connectAccount\\([\"']reddit[\"']\\)"
    - from: "connectAccount"
      to: "social_accounts INSERT"
      via: "service-role supabase client"
      pattern: "display_name.*konto"
---

<objective>
Drop the handle input from the connect flow, change `connectAccount` to a single-arg
signature `connectAccount(platform)`, auto-default `display_name` to
`"Reddit konto N"` / `"LinkedIn konto N"` (per-user-per-platform numbering), and
remove the `linkedin-${user.id.slice(0,8)}` placeholder fallback.

Implements D-04 (no handle input) and CONTEXT.md §Specifics ("user klika 'Dodaj konto'
i widzi ekran logowania").

Purpose: Zero-friction connect. Display label is system-generated; user can rename via
plan 17.8-04's pencil affordance after the fact.

Output: User clicks `Connect Reddit Account` (or `Connect LinkedIn Account`) → BB live
view opens immediately. No form, no validation, no friction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.8-account-identity-hygiene/17.8-CONTEXT.md
@.planning/phases/17.8-account-identity-hygiene/17.8-UI-SPEC.md
@.planning/phases/17.8-account-identity-hygiene/17.8-RESEARCH.md
@.planning/phases/17.8-account-identity-hygiene/17.8-03-SUMMARY.md
@CLAUDE.md
@src/features/accounts/components/connection-flow.tsx
@src/features/accounts/components/account-list.tsx
@src/features/accounts/actions/account-actions.ts
@src/features/browser-profiles/lib/allocator.ts

<interfaces>
After plan 03:
- `allocateBrowserProfile` accepts `profileLabel: string` (renamed from `handle`); free-form, no validation
- account-card.tsx renders display_name + identity_status state machine

Plan 03 explicitly does NOT touch `connectAccount`'s public signature or call sites — that's this plan's job.

Plan 03 does drop the handle-input form block from `account-list.tsx` (UI removal). This plan finishes the wiring: Connect button now calls `connectAccount(platform)` directly, and confirms the form is fully gone.

Auto-numbering query (per CONTEXT.md "Claude's Discretion" and UI-SPEC):
```ts
const { count } = await supabase
  .from("social_accounts")
  .select("id", { count: "exact", head: true })
  .eq("user_id", userId)
  .eq("platform", platform)
const N = (count ?? 0) + 1
const display_name = platform === "reddit" ? `Reddit konto ${N}` : `LinkedIn konto ${N}`
```

(Note: this is racy under concurrent connects from the same user, but the worst case is two `Reddit konto 2` rows — user can rename via pencil. CONTEXT.md explicitly allows duplicate display_names per `<deferred>`. No need for a sequence/transaction.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: connectAccount single-arg + auto-numbered display_name</name>
  <files>src/features/accounts/actions/account-actions.ts, src/features/accounts/actions/account-actions.test.ts</files>
  <read_first>
    - src/features/accounts/actions/account-actions.ts (current connectAccount, lines 32-51 specifically; placeholder fallback at line ~48-51)
    - src/features/browser-profiles/lib/allocator.ts (after plan 03 rename: profileLabel param)
    - .planning/phases/17.8-account-identity-hygiene/17.8-RESEARCH.md §3.A
  </read_first>
  <behavior>
    Tests:
    - connectAccount("reddit") with 0 existing reddit accounts for user → display_name="Reddit konto 1"
    - connectAccount("reddit") with 2 existing reddit accounts → display_name="Reddit konto 3"
    - connectAccount("linkedin") with 0 existing linkedin accounts → display_name="LinkedIn konto 1"
    - INSERT row has identity_status='pending', reddit_username=null, linkedin_slug=null
    - allocateBrowserProfile is called with profileLabel = the auto-generated display_name (or just `${platform}` — planner choice; pick display_name for traceability)
    - No `linkedin-${user.id.slice(0,8)}` literal anywhere in source
    - Old test cases that pass `connectAccount("reddit", "some_handle")` are updated to drop the handle arg
  </behavior>
  <action>
**A. Refactor `connectAccount` in account-actions.ts:**

Change signature from `connectAccount(platform: SocialPlatform, handle: string)` (or whatever it currently is) to:

```ts
export async function connectAccount(platform: "reddit" | "linkedin"): Promise<{ success: true; accountId: string } | { error: string }>
```

Inside the function:
1. Auth check (existing pattern)
2. Count existing rows for `(user_id, platform)`:
   ```ts
   const { count } = await supabase
     .from("social_accounts")
     .select("id", { count: "exact", head: true })
     .eq("user_id", user.id)
     .eq("platform", platform)
   const n = (count ?? 0) + 1
   const platformLabel = platform === "reddit" ? "Reddit" : "LinkedIn"
   const displayName = `${platformLabel} konto ${n}`
   ```
3. Allocate browser profile with `profileLabel: displayName` (the renamed param from plan 03)
4. INSERT into social_accounts with:
   - `display_name: displayName`
   - `identity_status: "pending"` (DB default also covers it; explicit for clarity)
   - `reddit_username: null`, `linkedin_slug: null` (DB defaults)
   - existing `browser_profile_id`, `platform`, `user_id` etc.
5. **Remove the LinkedIn `linkedin-${user.id.slice(0,8)}` placeholder fallback entirely.** No conditional based on platform — auto-numbered display_name is the sole label source.
6. Return `{ success: true, accountId: <inserted row id> }`

**B. Tests** — extend `account-actions.test.ts` (created/extended in plan 04) with the cases from `<behavior>`. Mock supabase. Mock `allocateBrowserProfile`.

Do NOT modify `verifyAccountSession` (already wired in plan 02).
  </action>
  <verify>
    <automated>pnpm test src/features/accounts/actions/account-actions && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `connectAccount` signature is `connectAccount(platform: "reddit" | "linkedin")` — single param
    - `grep -nE "linkedin-\\\${user\\.id" src/` returns 0 results (placeholder gone)
    - `grep -nE "Reddit konto|LinkedIn konto" src/features/accounts/actions/account-actions.ts` returns ≥ 2 (template strings present)
    - INSERT includes `display_name` and `identity_status: "pending"` (or relies on DB default with comment)
    - Tests cover: 0 existing → konto 1, 2 existing → konto 3, both platforms, identity columns null, allocator called with profileLabel
    - `pnpm test src/features/accounts/actions/account-actions` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>connectAccount is single-arg, auto-numbers display_name, leaves identity null+pending.</done>
</task>

<task type="auto">
  <name>Task 2: ConnectionFlow + AccountList wiring (drop handle input)</name>
  <files>src/features/accounts/components/connection-flow.tsx, src/features/accounts/components/account-list.tsx</files>
  <read_first>
    - .planning/phases/17.8-account-identity-hygiene/17.8-UI-SPEC.md §"Surface 1 — ConnectionFlow simplification"
    - src/features/accounts/components/connection-flow.tsx (current shape — handle prop, if any)
    - src/features/accounts/components/account-list.tsx (after plan 03 — form should already be removed; this task verifies and finishes the wiring)
    - .planning/phases/17.8-account-identity-hygiene/17.8-03-SUMMARY.md (confirm plan 03 dropped the form block)
  </read_first>
  <action>
**A. `connection-flow.tsx`**
- If `connection-flow.tsx` accepts a `handle` prop or threads it to `connectAccount`, remove it.
- Per UI-SPEC §Surface 1: structural layout (Step 1/2/3, iframe, retry, cancel) is UNCHANGED. Only the call to `connectAccount` changes — pass only `platform`.
- If the file already takes only `platform` and never threaded handle, this is a no-op for connection-flow.tsx itself.

**B. `account-list.tsx`**
- Verify plan 03 removed the `<form onSubmit={submitRedditHandle}>` block, the `handleInput` state, and `submitRedditHandle()` function. If anything remains, remove it.
- The two CTAs (`Connect Reddit Account`, `Connect LinkedIn Account`) call:
  ```ts
  function openConnectDialog(platform: "reddit" | "linkedin") {
    startTransition(async () => {
      const result = await connectAccount(platform)
      if ("error" in result) {
        toast.error(result.error)
        return
      }
      // mount ConnectionFlow with the new accountId / BB live view (existing logic)
    })
  }
  ```
  Remove any `handle` arg from the call. Same flow for both platforms — Reddit was the only one with a form.

- `<Button onClick={() => openConnectDialog("reddit")}>Connect Reddit Account</Button>`
- `<Button onClick={() => openConnectDialog("linkedin")}>Connect LinkedIn Account</Button>`

Loading state copy unchanged ("Setting up your account..."). Iframe live view unchanged.

Do NOT touch ConnectionFlow's iframe/retry/cancel logic — UI-SPEC explicitly says it is preserved structurally.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint src/features/accounts/components</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "submitRedditHandle|handleInput" src/features/accounts/components/account-list.tsx src/features/accounts/components/connection-flow.tsx` returns 0
    - `grep -nE "<Input[^>]*placeholder=[\"']u/" src/features/accounts/components` returns 0 (no Reddit handle input)
    - `grep -nE "connectAccount\([\"']reddit[\"']\)" src/features/accounts/components/account-list.tsx` returns ≥ 1 (single-arg call)
    - `grep -nE "connectAccount\([\"']linkedin[\"']\)" src/features/accounts/components/account-list.tsx` returns ≥ 1
    - `grep -nE "connectAccount\([^,)]+,[^)]+\)" src/features/accounts` returns 0 (no two-arg calls remain anywhere)
    - `pnpm typecheck` exits 0
    - `pnpm lint src/features/accounts/components` exits 0
  </acceptance_criteria>
  <done>ConnectionFlow + AccountList have zero handle-input affordance; both CTAs call connectAccount with platform only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → connectAccount | Only `platform` literal crosses (`"reddit" \| "linkedin"`); server validates against the union; no user-provided handle reaches the DB anymore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.8-10 | Tampering | platform arg manipulated | mitigate | Server action validates `platform` against the union literal; any other value is rejected |
| T-17.8-11 | Repudiation | Auto-numbered display_name collides between concurrent requests from same user | accept | Per CONTEXT.md `<deferred>`, duplicate display_names are explicitly allowed (user can rename via pencil); race produces at most two identical labels, not a security issue |
</threat_model>

<verification>
- `pnpm typecheck` clean
- `pnpm test src/features/accounts/actions/account-actions` green
- Manual UAT-01 / UAT-02 from VALIDATION.md verify the no-input flow end-to-end
- `grep -rn "connectAccount\(.*,.*\)" src/` shows no two-arg call sites remain
</verification>

<success_criteria>
- `connectAccount(platform)` is single-arg
- No handle input field exists in any UI surface
- Auto-numbered display_name (`Reddit konto N` / `LinkedIn konto N`) on insert
- LinkedIn placeholder `linkedin-${user.id.slice(0,8)}` removed
- Allocator receives display_name as `profileLabel`
- Tests cover the new shape; existing tests refit
</success_criteria>

<output>
Create `.planning/phases/17.8-account-identity-hygiene/17.8-05-SUMMARY.md` with:
- New connectAccount signature
- Auto-numbering algorithm + race tradeoff
- Removal of handle input form + LinkedIn placeholder
- Cross-references for UAT-01 (Reddit no-input flow) and UAT-02 (LinkedIn no-input flow)
</output>
